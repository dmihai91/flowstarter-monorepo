

======================================================================
// REFERENCE: consultant-pro/components/Header.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
const { header } = siteLabels;
const base = import.meta.env.BASE_URL;
---

<header class="site-header" id="site-header">
  <div class="site-header__inner container">
    <a href={base} class="site-header__logo">
      <span class="site-header__logo-text">{header.logo}</span>
    </a>

    <nav class="site-header__nav" aria-label="Main navigation">
      <ul class="site-header__nav-list">
        {header.navLinks.map(link => (
          <li>
            <a href={`${base}${link.href.replace(/^\//, '')}`} class="site-header__nav-link">
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <a href={`${base}contact`} class="site-header__cta">Book a Call</a>

    <button class="site-header__menu-btn" aria-label="Open menu" aria-expanded="false" data-menu-btn>
      <span class="site-header__menu-bar"></span>
      <span class="site-header__menu-bar"></span>
      <span class="site-header__menu-bar"></span>
    </button>
  </div>
</header>

<!-- Mobile Drawer -->
<div class="mobile-drawer" data-mobile-drawer aria-hidden="true">
  <div class="mobile-drawer__backdrop" data-drawer-backdrop></div>
  <div class="mobile-drawer__panel">
    <button class="mobile-drawer__close" aria-label="Close menu" data-drawer-close>
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <line x1="18" y1="6" x2="6" y2="18"/>
        <line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
    <nav class="mobile-drawer__nav" aria-label="Mobile navigation">
      <ul class="mobile-drawer__nav-list">
        {header.navLinks.map(link => (
          <li>
            <a href={`${base}${link.href.replace(/^\//, '')}`} class="mobile-drawer__nav-link">
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    <a href={`${base}contact`} class="mobile-drawer__cta">Book a Discovery Call</a>
  </div>
</div>

<style>
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: rgba(15, 17, 23, 0.92);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border-color);
    height: 72px;
    display: flex;
    align-items: center;
  }

  .site-header__inner {
    display: flex;
    align-items: center;
    gap: 40px;
  }

  .site-header__logo {
    flex-shrink: 0;
    display: flex;
    align-items: center;
  }

  .site-header__logo-text {
    font-family: var(--font-title);
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--brand-primary);
    letter-spacing: 0.02em;
  }

  .site-header__nav {
    flex: 1;
  }

  .site-header__nav-list {
    display: flex;
    gap: 36px;
    align-items: center;
  }

  .site-header__nav-link {
    font-size: var(--fs-small);
    font-weight: var(--fw-medium);
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: color 0.2s ease;
  }

  .site-header__nav-link:hover {
    color: var(--text-primary);
  }

  .site-header__cta {
    flex-shrink: 0;
    padding: 10px 22px;
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    color: var(--brand-primary);
    border: 1px solid var(--border-gold);
    transition: all 0.25s ease;
    font-family: var(--font-button);
  }

  .site-header__cta:hover {
    background: var(--brand-primary);
    color: #0f1117;
  }

  .site-header__menu-btn {
    display: none;
    flex-direction: column;
    gap: 5px;
    padding: 8px;
    margin-left: auto;
  }

  .site-header__menu-bar {
    display: block;
    width: 22px;
    height: 1.5px;
    background: var(--text-primary);
    transition: all 0.3s ease;
  }

  /* Mobile drawer */
  .mobile-drawer {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 200;
  }

  .mobile-drawer.is-open {
    display: block;
  }

  .mobile-drawer__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(8, 10, 14, 0.8);
    backdrop-filter: blur(4px);
  }

  .mobile-drawer__panel {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    width: min(320px, 85vw);
    background: var(--bg-card);
    border-left: 1px solid var(--border-color);
    display: flex;
    flex-direction: column;
    padding: 32px 24px;
    gap: 32px;
    transform: translateX(100%);
    transition: transform 0.3s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .mobile-drawer.is-open .mobile-drawer__panel {
    transform: translateX(0);
  }

  .mobile-drawer__close {
    align-self: flex-end;
    color: var(--text-secondary);
    padding: 8px;
    transition: color 0.2s;
  }

  .mobile-drawer__close:hover {
    color: var(--text-primary);
  }

  .mobile-drawer__nav-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .mobile-drawer__nav-link {
    display: block;
    padding: 14px 0;
    font-size: 1.1rem;
    font-weight: var(--fw-medium);
    color: var(--text-secondary);
    border-bottom: 1px solid var(--border-color);
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: color 0.2s;
  }

  .mobile-drawer__nav-link:hover {
    color: var(--brand-primary);
  }

  .mobile-drawer__cta {
    display: block;
    text-align: center;
    padding: 14px 24px;
    background: var(--brand-primary);
    color: #0f1117;
    font-weight: var(--fw-bold);
    font-size: var(--fs-small);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    font-family: var(--font-button);
    margin-top: auto;
    transition: opacity 0.25s ease;
  }

  .mobile-drawer__cta:hover {
    opacity: 0.88;
  }

  @media (max-width: 900px) {
    .site-header__nav,
    .site-header__cta {
      display: none;
    }

    .site-header__menu-btn {
      display: flex;
    }
  }
</style>

<script>
  const menuBtn = document.querySelector('[data-menu-btn]');
  const drawer = document.querySelector('[data-mobile-drawer]');
  const backdrop = document.querySelector('[data-drawer-backdrop]');
  const closeBtn = document.querySelector('[data-drawer-close]');

  const openDrawer = () => {
    drawer?.classList.add('is-open');
    drawer?.setAttribute('aria-hidden', 'false');
    menuBtn?.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  };

  const closeDrawer = () => {
    drawer?.classList.remove('is-open');
    drawer?.setAttribute('aria-hidden', 'true');
    menuBtn?.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  };

  menuBtn?.addEventListener('click', openDrawer);
  backdrop?.addEventListener('click', closeDrawer);
  closeBtn?.addEventListener('click', closeDrawer);

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeDrawer();
  });
</script>


======================================================================
// REFERENCE: consultant-pro/components/Hero.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import ActionButton from './design-system/ActionButton.astro';
const { hero, stats } = siteLabels;
const base = import.meta.env.BASE_URL;
---

<section class="hero">
  <div class="hero__grid container">
    <div class="hero__content">
      <p class="hero__eyebrow" data-hero-animate style="--delay:0ms">{hero.eyebrow}</p>
      <h1 class="hero__title" data-hero-animate style="--delay:120ms">{hero.title}</h1>
      <p class="hero__subtitle" data-hero-animate style="--delay:240ms">{hero.subtitle}</p>
      <div class="hero__actions" data-hero-animate style="--delay:360ms">
        <ActionButton href={`${base}contact`} label={hero.cta_primary} variant="gold" />
        <ActionButton href={`${base}services`} label={hero.cta_secondary} variant="outline-light" />
      </div>
    </div>
    <div class="hero__visual" aria-hidden="true">
      <svg class="hero__diamonds" viewBox="0 0 400 400" fill="none" xmlns="http://www.w3.org/2000/svg">
        <g class="hero__diamond-outer">
          <polygon points="200,10 390,200 200,390 10,200" stroke="#C9A84C" stroke-width="0.8" fill="none" opacity="0.6"/>
        </g>
        <g class="hero__diamond-mid">
          <polygon points="200,60 340,200 200,340 60,200" stroke="#C9A84C" stroke-width="0.8" fill="none" opacity="0.45"/>
        </g>
        <g class="hero__diamond-inner">
          <polygon points="200,110 290,200 200,290 110,200" stroke="#C9A84C" stroke-width="0.8" fill="none" opacity="0.3"/>
        </g>
        <line x1="200" y1="10" x2="200" y2="390" stroke="#C9A84C" stroke-width="0.4" opacity="0.15"/>
        <line x1="10" y1="200" x2="390" y2="200" stroke="#C9A84C" stroke-width="0.4" opacity="0.15"/>
        <circle cx="200" cy="200" r="3" fill="#C9A84C" opacity="0.8"/>
        <circle cx="200" cy="200" r="8" stroke="#C9A84C" stroke-width="0.5" fill="none" opacity="0.4"/>
        <circle cx="200" cy="200" r="20" stroke="#C9A84C" stroke-width="0.3" fill="none" opacity="0.2"/>
      </svg>
    </div>
  </div>

  <div class="hero__stats">
    <div class="container">
      <div class="hero__stats-inner">
        {stats.items.slice(0, 4).map((s) => (
          <div class="hero__stat">
            <span class="hero__stat-number" data-stat-number data-target={s.number}>{s.number}</span>
            <span class="hero__stat-label">{s.label}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    background: var(--bg-dark);
    display: flex;
    flex-direction: column;
  }

  .hero__grid {
    flex: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
    padding-top: 120px;
    padding-bottom: 80px;
  }

  .hero__eyebrow {
    font-size: var(--fs-small);
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--brand-primary);
    margin-bottom: 24px;
    font-family: var(--font-body);
    font-weight: var(--fw-medium);
  }

  .hero__title {
    font-size: var(--fs-display);
    font-family: var(--font-title);
    font-weight: 700;
    color: var(--text-primary);
    line-height: 1.08;
    letter-spacing: -0.02em;
    margin-bottom: 28px;
  }

  .hero__subtitle {
    font-size: 1.1rem;
    line-height: 1.65;
    color: var(--text-secondary);
    max-width: 480px;
    margin-bottom: 40px;
  }

  .hero__actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .hero__visual {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero__diamonds {
    width: 100%;
    max-width: 420px;
  }

  .hero__diamond-outer {
    transform-origin: 200px 200px;
    animation: rotate-cw 40s linear infinite;
  }

  .hero__diamond-mid {
    transform-origin: 200px 200px;
    animation: rotate-ccw 28s linear infinite;
  }

  .hero__diamond-inner {
    transform-origin: 200px 200px;
    animation: rotate-cw 20s linear infinite;
  }

  @keyframes rotate-cw {
    to { transform: rotate(360deg); }
  }

  @keyframes rotate-ccw {
    to { transform: rotate(-360deg); }
  }

  /* Entry animation */
  [data-hero-animate] {
    opacity: 0;
    transform: translateY(24px);
    animation: hero-in 700ms cubic-bezier(0.22, 1, 0.36, 1) forwards;
    animation-delay: var(--delay, 0ms);
  }

  @keyframes hero-in {
    to {
      opacity: 1;
      transform: none;
    }
  }

  /* Stats strip */
  .hero__stats {
    border-top: 1px solid var(--border-color);
    background: var(--bg-card);
  }

  .hero__stats-inner {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }

  .hero__stat {
    padding: 28px 24px;
    border-right: 1px solid var(--border-color);
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .hero__stat:last-child {
    border-right: none;
  }

  .hero__stat-number {
    font-family: var(--font-title);
    font-size: clamp(1.6rem, 2.5vw, 2.4rem);
    font-weight: 700;
    color: var(--brand-primary);
    line-height: 1;
  }

  .hero__stat-label {
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: var(--text-secondary);
  }

  @media (max-width: 1024px) {
    .hero__grid {
      grid-template-columns: 1fr;
    }

    .hero__visual {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .hero__stats-inner {
      grid-template-columns: repeat(2, 1fr);
    }

    .hero__stat:nth-child(2) {
      border-right: none;
    }

    .hero__stat:nth-child(3) {
      border-top: 1px solid var(--border-color);
    }

    .hero__stat:nth-child(4) {
      border-top: 1px solid var(--border-color);
      border-right: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__diamond-outer,
    .hero__diamond-mid,
    .hero__diamond-inner {
      animation: none;
    }

    [data-hero-animate] {
      opacity: 1;
      transform: none;
      animation: none;
    }
  }
</style>

<script>
  // Stat count-up animation
  const statEls = document.querySelectorAll('[data-stat-number]');
  const obs = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (!e.isIntersecting) return;
      const el = e.target as HTMLElement;
      const raw = (el as HTMLElement).dataset.target || '0';
      const num = parseFloat(raw.replace(/[^0-9.]/g, ''));
      const suffix = raw.replace(/[0-9.]/g, '');
      const dur = 1200;
      const t0 = performance.now();
      const tick = (t: number) => {
        const p = Math.min((t - t0) / dur, 1);
        const ease = 1 - Math.pow(1 - p, 3);
        el.textContent = Math.round(num * ease) + suffix;
        if (p < 1) requestAnimationFrame(tick);
      };
      requestAnimationFrame(tick);
      obs.unobserve(el);
    });
  }, { threshold: 0.5 });
  statEls.forEach(el => obs.observe(el));
</script>


======================================================================
// REFERENCE: consultant-pro/components/Services.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import SectionBlock from './design-system/SectionBlock.astro';
import SectionHeading from './design-system/SectionHeading.astro';
const { services } = siteLabels;
---

<SectionBlock id="services" bg="dark" padding="default">
  <div class="services__inner container">
    <div class="services__header">
      <SectionHeading
        label={services.sectionLabel}
        title={services.sectionTitle}
        align="center"
      />
    </div>
    <div class="services__grid">
      {services.items.map((item, i) => (
        <div class="services__card">
          <span class="services__card-number">0{i + 1}</span>
          <h3 class="services__card-title">{item.title}</h3>
          <p class="services__card-desc">{item.description}</p>
          <span class="services__card-arrow" aria-hidden="true">&#8599;</span>
        </div>
      ))}
    </div>
  </div>
</SectionBlock>

<style>
  .services__inner {
    display: flex;
    flex-direction: column;
    gap: 56px;
  }

  .services__header {
    text-align: center;
  }

  .services__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border-top: 1px solid var(--border-color);
    border-left: 1px solid var(--border-color);
  }

  .services__card {
    position: relative;
    padding: 40px 32px;
    border-right: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
    background: var(--bg-dark);
    transition: background 0.25s ease;
    display: flex;
    flex-direction: column;
    gap: 16px;
    overflow: hidden;
  }

  .services__card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--brand-primary);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.35s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .services__card:hover {
    background: var(--bg-card-hover);
  }

  .services__card:hover::before {
    transform: scaleX(1);
  }

  .services__card-number {
    font-family: var(--font-title);
    font-size: var(--fs-small);
    color: var(--brand-primary);
    font-weight: var(--fw-medium);
    letter-spacing: 2px;
  }

  .services__card-title {
    font-family: var(--font-title);
    font-size: 1.2rem;
    font-weight: var(--fw-semibold);
    color: var(--text-primary);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    line-height: 1.2;
  }

  .services__card-desc {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.6;
    flex: 1;
  }

  .services__card-arrow {
    position: absolute;
    bottom: 20px;
    right: 24px;
    font-size: 1.4rem;
    color: var(--brand-primary);
    opacity: 0;
    transform: translate(-4px, 4px);
    transition: all 0.25s ease;
  }

  .services__card:hover .services__card-arrow {
    opacity: 1;
    transform: translate(0, 0);
  }

  @media (max-width: 900px) {
    .services__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 600px) {
    .services__grid {
      grid-template-columns: 1fr;
    }
  }
</style>


======================================================================
// REFERENCE: consultant-pro/components/Process.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import SectionBlock from './design-system/SectionBlock.astro';
import SectionHeading from './design-system/SectionHeading.astro';
const { process } = siteLabels;
---

<SectionBlock id="process" bg="darker" padding="default" topBorder={true}>
  <div class="process__inner container">
    <div class="process__left">
      <div class="process__heading-sticky">
        <SectionHeading
          label={process.sectionLabel}
          title={process.sectionTitle}
          maxWidth="320px"
        />
        <p class="process__lead">A disciplined approach that cuts through noise and delivers decisions you can execute on.</p>
      </div>
    </div>
    <div class="process__right is-intro-state" data-process-list>
      {process.items.map((item, index) => (
        <div class={`process__item ${index === 0 ? 'process__item--first' : ''}`}>
          <span class="process__item-number">{item.number}</span>
          <div class="process__item-content">
            <h3 class="process__item-title">{item.title}</h3>
            <p class="process__item-desc">{item.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</SectionBlock>

<style>
  .process__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: start;
  }

  .process__heading-sticky {
    position: sticky;
    top: 100px;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .process__lead {
    font-size: var(--fs-small);
    color: var(--text-muted);
    line-height: 1.65;
    max-width: 300px;
  }

  .process__right {
    display: flex;
    flex-direction: column;
  }

  .process__item {
    display: flex;
    gap: 24px;
    padding: 28px 0;
    border-bottom: 1px solid var(--border-color);
    align-items: flex-start;
  }

  .process__item:last-child {
    padding-bottom: 0;
    border-bottom: none;
  }

  .process__item--first {
    padding-top: 0;
  }

  .process__item-number {
    font-size: var(--fs-xs);
    color: var(--brand-primary);
    font-weight: var(--fw-medium);
    min-width: 28px;
    padding-top: 3px;
    letter-spacing: 1px;
    font-family: var(--font-body);
  }

  .process__item-content {
    flex: 1;
    filter: blur(4px);
    opacity: 0.68;
    transition:
      filter 260ms ease,
      opacity 260ms ease,
      transform 260ms ease;
    transform: translateX(0);
  }

  .process__item:hover .process__item-content,
  .process__item:focus-within .process__item-content {
    filter: blur(0);
    opacity: 1;
    transform: translateX(6px);
  }

  /* First item unblurred on intro */
  .process__right.is-intro-state .process__item--first .process__item-content {
    filter: blur(0);
    opacity: 1;
  }

  .process__item-title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    font-weight: var(--fw-semibold);
    color: var(--text-primary);
    margin-bottom: 8px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .process__item-desc {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .process__inner {
      grid-template-columns: 1fr;
      gap: 36px;
    }

    .process__heading-sticky {
      position: static;
    }

    .process__item-content {
      filter: blur(0);
      opacity: 1;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .process__item-content {
      transition: none;
      transform: none;
      filter: blur(0);
      opacity: 1;
    }

    .process__item:hover .process__item-content,
    .process__item:focus-within .process__item-content {
      transform: none;
    }
  }
</style>

<script>
  const processList = document.querySelector('[data-process-list]');

  if (processList) {
    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
    let introPlayed = false;

    const clearIntroState = () => {
      processList.classList.remove('is-intro-state');
    };

    if (reduceMotion.matches) {
      clearIntroState();
    } else {
      const observer = new IntersectionObserver(
        ([entry]) => {
          if (!entry.isIntersecting || introPlayed) return;

          introPlayed = true;
          processList.classList.add('is-intro-state');

          window.setTimeout(() => {
            clearIntroState();
          }, 1400);

          observer.disconnect();
        },
        { threshold: 0.35 }
      );

      observer.observe(processList);
    }
  }
</script>


======================================================================
// REFERENCE: consultant-pro/components/Testimonials.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import SectionBlock from './design-system/SectionBlock.astro';
import SectionHeading from './design-system/SectionHeading.astro';
const { testimonials } = siteLabels;
---

<SectionBlock id="testimonials" bg="dark" padding="default" topBorder={true}>
  <div class="testimonials__inner container">
    <div class="testimonials__header">
      <SectionHeading
        label="Client Results"
        title="WHAT CLIENTS SAY"
        align="center"
      />
    </div>
    <div class="testimonials__grid">
      {testimonials.map((t, i) => (
        <div class="testimonials__card is-observing" data-testimonial style={`--stagger-delay: ${i * 100}ms`}>
          <span class="testimonials__quote-mark" aria-hidden="true">&ldquo;</span>
          <blockquote class="testimonials__quote">{t.quote}</blockquote>
          <footer class="testimonials__author">
            <span class="testimonials__author-name">{t.authorName}</span>
            <span class="testimonials__author-role">{t.authorRole}</span>
          </footer>
        </div>
      ))}
    </div>
  </div>
</SectionBlock>

<style>
  .testimonials__inner {
    display: flex;
    flex-direction: column;
    gap: 56px;
  }

  .testimonials__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }

  .testimonials__card {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    padding: 40px 36px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    position: relative;
    transition: border-color 0.25s ease, background 0.25s ease;
  }

  .testimonials__card:hover {
    border-color: var(--border-gold);
    background: var(--bg-card-hover);
  }

  /* Reveal animation via useVisibilityClass */
  .testimonials__card.is-observing {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 600ms cubic-bezier(0.22, 1, 0.36, 1) var(--stagger-delay, 0ms),
      transform 600ms cubic-bezier(0.22, 1, 0.36, 1) var(--stagger-delay, 0ms),
      border-color 0.25s ease,
      background 0.25s ease;
  }

  .testimonials__card.is-visible {
    opacity: 1;
    transform: none;
  }

  .testimonials__quote-mark {
    font-family: var(--font-title);
    font-size: 5rem;
    color: var(--brand-primary);
    line-height: 0.7;
    display: block;
    opacity: 0.8;
  }

  .testimonials__quote {
    font-family: var(--font-title);
    font-size: 1.05rem;
    font-style: italic;
    color: var(--text-primary);
    line-height: 1.7;
    flex: 1;
  }

  .testimonials__author {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding-top: 16px;
    border-top: 1px solid var(--border-color);
  }

  .testimonials__author-name {
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    color: var(--brand-primary);
    text-transform: uppercase;
    letter-spacing: 1.5px;
    font-family: var(--font-body);
  }

  .testimonials__author-role {
    font-size: var(--fs-xs);
    color: var(--text-muted);
    letter-spacing: 0.5px;
  }

  @media (max-width: 768px) {
    .testimonials__grid {
      grid-template-columns: 1fr;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .testimonials__card.is-observing {
      opacity: 1;
      transform: none;
      transition: border-color 0.25s ease, background 0.25s ease;
    }
  }
</style>

<script>
  import { useVisibilityClass } from '../scripts/useVisibilityClass.js';

  useVisibilityClass({
    selector: '[data-testimonial]',
    className: 'is-visible',
    observingClassName: 'is-observing',
    threshold: 0.2,
    once: true,
  });
</script>


======================================================================
// REFERENCE: consultant-pro/components/Stats.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
const { stats } = siteLabels;
---

<section class="stats-section">
  <div class="stats-section__inner container">
    {stats.items.map((item) => (
      <div class="stats-section__item">
        <span
          class="stats-section__number"
          data-count-up
          data-target={item.number}
        >
          {item.number}
        </span>
        <span class="stats-section__label">{item.label}</span>
      </div>
    ))}
  </div>
</section>

<style>
  .stats-section {
    background: var(--bg-card);
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
    padding: 60px 0;
  }

  .stats-section__inner {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
  }

  .stats-section__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    padding: 24px;
    border-right: 1px solid var(--border-color);
    text-align: center;
  }

  .stats-section__item:last-child {
    border-right: none;
  }

  .stats-section__number {
    font-family: var(--font-title);
    font-size: clamp(2rem, 3.5vw, 3.2rem);
    font-weight: 700;
    color: var(--brand-primary);
    line-height: 1;
  }

  .stats-section__label {
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--text-secondary);
  }

  @media (max-width: 768px) {
    .stats-section__inner {
      grid-template-columns: repeat(2, 1fr);
    }

    .stats-section__item:nth-child(2) {
      border-right: none;
    }

    .stats-section__item:nth-child(3) {
      border-top: 1px solid var(--border-color);
    }

    .stats-section__item:nth-child(4) {
      border-top: 1px solid var(--border-color);
      border-right: none;
    }
  }
</style>

<script>
  const countEls = document.querySelectorAll('[data-count-up]');
  const obs = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (!e.isIntersecting) return;
      const el = e.target as HTMLElement;
      const raw = el.dataset.target || '0';
      const num = parseFloat(raw.replace(/[^0-9.]/g, ''));
      const suffix = raw.replace(/[0-9.]/g, '');
      const dur = 1400;
      const t0 = performance.now();
      const tick = (t: number) => {
        const p = Math.min((t - t0) / dur, 1);
        const ease = 1 - Math.pow(1 - p, 3);
        el.textContent = Math.round(num * ease) + suffix;
        if (p < 1) requestAnimationFrame(tick);
      };
      requestAnimationFrame(tick);
      obs.unobserve(el);
    });
  }, { threshold: 0.5 });
  countEls.forEach(el => obs.observe(el));
</script>


======================================================================
// REFERENCE: consultant-pro/components/CTA.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import ActionButton from './design-system/ActionButton.astro';
const { cta } = siteLabels;
const base = import.meta.env.BASE_URL;
---

<section class="cta-section">
  <div class="cta-section__inner container">
    <h2 class="cta-section__title">{cta.title}</h2>
    <p class="cta-section__desc">{cta.description}</p>
    <div class="cta-section__actions">
      <ActionButton href={`${base}contact`} label={cta.primary} variant="primary" />
      <ActionButton href={`${base}services`} label={cta.secondary} variant="outline" />
    </div>
  </div>
</section>

<style>
  .cta-section {
    background: var(--brand-primary);
    padding: 80px 0;
  }

  .cta-section__inner {
    max-width: 720px;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 24px;
  }

  .cta-section__title {
    font-family: var(--font-title);
    font-size: var(--fs-h2);
    font-weight: 700;
    color: #0f1117;
    line-height: 1.1;
  }

  .cta-section__desc {
    color: rgba(15, 17, 23, 0.72);
    max-width: 520px;
    font-size: var(--fs-body);
  }

  .cta-section__actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    justify-content: center;
  }

  /* ActionButton overrides for gold background */
  .cta-section :global(.ds-action-button--primary) {
    background: #0f1117;
    color: var(--brand-primary);
    border-color: #0f1117;
  }

  .cta-section :global(.ds-action-button--primary:hover) {
    opacity: 0.88;
  }

  .cta-section :global(.ds-action-button--outline) {
    color: #0f1117;
    border-color: rgba(15, 17, 23, 0.4);
  }

  .cta-section :global(.ds-action-button--outline:hover) {
    background: #0f1117;
    color: var(--brand-primary);
    border-color: #0f1117;
  }
</style>


======================================================================
// REFERENCE: consultant-pro/components/Footer.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
const { footer, header } = siteLabels;
const base = import.meta.env.BASE_URL;
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="site-footer__inner container">
    <div class="site-footer__top">
      <div class="site-footer__brand">
        <a href={base} class="site-footer__logo">{footer.logo}</a>
        <p class="site-footer__tagline">{footer.tagline}</p>
      </div>

      <div class="site-footer__nav-col">
        <h4 class="site-footer__col-title">Navigation</h4>
        <ul class="site-footer__nav-list">
          {header.navLinks.map(link => (
            <li>
              <a href={`${base}${link.href.replace(/^\//, '')}`} class="site-footer__nav-link">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div class="site-footer__contact-col">
        <h4 class="site-footer__col-title">Get in Touch</h4>
        <ul class="site-footer__contact-list">
          <li>
            <a href={`${base}contact`} class="site-footer__nav-link">Book a Discovery Call &rarr;</a>
          </li>
          <li>
            <a href={`${base}services`} class="site-footer__nav-link">View Engagements &rarr;</a>
          </li>
          <li>
            <a href={`${base}about`} class="site-footer__nav-link">About Me &rarr;</a>
          </li>
        </ul>
      </div>
    </div>

    <div class="site-footer__bottom">
      <p class="site-footer__copy">&copy; {year} {footer.copyrightSuffix}</p>
      <p class="site-footer__built">Strategy without compromise.</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--bg-darker);
    border-top: 1px solid var(--border-gold);
  }

  .site-footer__inner {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .site-footer__top {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 48px;
    padding: 64px 0 48px;
    border-bottom: 1px solid var(--border-color);
  }

  .site-footer__logo {
    font-family: var(--font-title);
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--brand-primary);
    display: inline-block;
    margin-bottom: 16px;
    letter-spacing: 0.02em;
  }

  .site-footer__tagline {
    font-size: var(--fs-small);
    color: var(--text-muted);
    line-height: 1.65;
    max-width: 300px;
  }

  .site-footer__col-title {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--text-muted);
    margin-bottom: 20px;
  }

  .site-footer__nav-list,
  .site-footer__contact-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .site-footer__nav-link {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    transition: color 0.2s ease;
  }

  .site-footer__nav-link:hover {
    color: var(--brand-primary);
  }

  .site-footer__bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 24px 0;
    gap: 16px;
    flex-wrap: wrap;
  }

  .site-footer__copy {
    font-size: var(--fs-xs);
    color: var(--text-muted);
  }

  .site-footer__built {
    font-size: var(--fs-xs);
    color: var(--text-muted);
    font-style: italic;
  }

  @media (max-width: 768px) {
    .site-footer__top {
      grid-template-columns: 1fr;
      gap: 36px;
    }

    .site-footer__bottom {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>


======================================================================
// REFERENCE: consultant-pro/components/design-system/ActionButton.astro
======================================================================
---
interface Props {
  href: string;
  label: string;
  variant?: 'primary' | 'outline' | 'gold' | 'outline-light';
  className?: string;
}
const { href, label, variant = 'primary', className = '' } = Astro.props;
---
<a href={href} class={`ds-action-button ds-action-button--${variant} ${className}`.trim()}>
  <span>{label}</span>
</a>
<style>
  .ds-action-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 14px 28px;
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    border: 1px solid transparent;
    transition: all 0.25s ease;
    white-space: nowrap;
    font-family: var(--font-button);
  }

  .ds-action-button--primary {
    background: var(--brand-primary);
    color: #0f1117;
    border-color: var(--brand-primary);
  }

  .ds-action-button--primary:hover {
    opacity: 0.88;
  }

  .ds-action-button--outline {
    background: transparent;
    color: var(--text-primary);
    border-color: var(--border-color);
  }

  .ds-action-button--outline:hover {
    border-color: var(--brand-primary);
    color: var(--brand-primary);
  }

  .ds-action-button--outline-light {
    background: transparent;
    color: var(--text-on-dark);
    border-color: rgba(255, 255, 255, 0.3);
  }

  .ds-action-button--outline-light:hover {
    border-color: var(--text-on-dark);
  }

  .ds-action-button--gold {
    background: var(--brand-primary);
    color: #0f1117;
    border-color: var(--brand-primary);
    font-weight: var(--fw-bold);
  }

  .ds-action-button--gold:hover {
    opacity: 0.88;
  }
</style>


======================================================================
// REFERENCE: consultant-pro/components/design-system/SectionBlock.astro
======================================================================
---
interface Props {
  id?: string;
  className?: string;
  bg?: 'dark' | 'darker' | 'card' | 'transparent';
  padding?: 'default' | 'compact' | 'tight' | 'none';
  topBorder?: boolean;
}

const {
  id,
  className = '',
  bg = 'dark',
  padding = 'default',
  topBorder = false,
} = Astro.props;
---

<section
  id={id}
  class={`ds-section ds-section--bg-${bg} ds-section--pad-${padding} ${topBorder ? 'ds-section--top-border' : ''} ${className}`.trim()}
>
  <slot />
</section>

<style>
  .ds-section {
    width: 100%;
  }

  .ds-section--pad-default {
    padding: var(--section-padding);
  }

  .ds-section--pad-compact {
    padding: 60px 0;
  }

  .ds-section--pad-tight {
    padding: 40px 0;
  }

  .ds-section--pad-none {
    padding: 0;
  }

  .ds-section--bg-dark {
    background: var(--bg-dark);
  }

  .ds-section--bg-darker {
    background: var(--bg-darker);
  }

  .ds-section--bg-card {
    background: var(--bg-card);
  }

  .ds-section--bg-transparent {
    background: transparent;
  }

  .ds-section--top-border {
    border-top: 1px solid var(--border-color);
  }
</style>


======================================================================
// REFERENCE: consultant-pro/components/design-system/SectionHeading.astro
======================================================================
---
interface Props {
  label?: string;
  title?: string;
  align?: 'left' | 'center';
  className?: string;
  maxWidth?: string;
}

const {
  label,
  title,
  align = 'left',
  className = '',
  maxWidth,
} = Astro.props;

const titleStyle = maxWidth ? `max-width:${maxWidth};` : undefined;
---

<div class={`ds-section-heading ds-section-heading--${align} ${className}`.trim()}>
  {label && <p class="section-label">{label}</p>}
  {title && <h2 class="section-title" style={titleStyle}>{title}</h2>}
</div>

<style>
  .ds-section-heading--center {
    text-align: center;
  }

  .ds-section-heading--center .section-title {
    margin-left: auto;
    margin-right: auto;
  }
</style>


======================================================================
// REFERENCE: consultant-pro/components/widgets/BookingWidget.astro
======================================================================
---
interface Props {
  title?: string;
  description?: string;
  buttonLabel?: string;
  className?: string;
}
const {
  title = 'Book a Discovery Call',
  description = "Let's spend 30 minutes on your situation. No pitch — just a direct conversation.",
  buttonLabel = 'Book a Call',
  className = '',
} = Astro.props;
---

<div class={`booking-widget ${className}`}
     data-project-id="{{PROJECT_ID}}"
     data-booking-url="{{BOOKING_URL}}">
  <div class="booking-widget__inner container">
    <div class="booking-widget__icon" aria-hidden="true">
      <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
        <rect x="3" y="4" width="18" height="18" rx="2"/>
        <line x1="3" y1="9" x2="21" y2="9"/>
        <line x1="8" y1="2" x2="8" y2="6"/>
        <line x1="16" y1="2" x2="16" y2="6"/>
      </svg>
    </div>
    <div class="booking-widget__copy">
      <h3 class="booking-widget__title">{title}</h3>
      <p class="booking-widget__desc">{description}</p>
    </div>
    <div class="booking-widget__action">
      <a href="#" class="booking-widget__btn" data-booking-trigger>{buttonLabel}</a>
      <p class="booking-widget__placeholder" hidden>
        Booking will be configured in your dashboard.
      </p>
    </div>
  </div>
</div>

<script>
  document.querySelectorAll('[data-booking-trigger]').forEach(btn => {
    const widget = btn.closest('[data-booking-url]');
    const url = widget?.dataset?.bookingUrl;
    const placeholder = widget?.querySelector('.booking-widget__placeholder');
    if (url && url !== '{{BOOKING_URL}}' && url.startsWith('http')) {
      btn.setAttribute('href', url);
      btn.setAttribute('target', '_blank');
      btn.setAttribute('rel', 'noopener noreferrer');
    } else {
      btn.style.display = 'none';
      if (placeholder) placeholder.hidden = false;
    }
  });
</script>

<style>
  .booking-widget {
    background: var(--bg-card);
    border: 1px solid var(--border-gold);
    padding: 56px 0;
  }

  .booking-widget__inner {
    display: flex;
    align-items: center;
    gap: 40px;
  }

  .booking-widget__icon {
    color: var(--brand-primary);
    flex-shrink: 0;
  }

  .booking-widget__copy {
    flex: 1;
  }

  .booking-widget__title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    color: var(--text-primary);
    margin-bottom: 10px;
  }

  .booking-widget__desc {
    color: var(--text-secondary);
    font-size: var(--fs-body);
    line-height: 1.6;
  }

  .booking-widget__action {
    flex-shrink: 0;
  }

  .booking-widget__btn {
    display: inline-flex;
    align-items: center;
    padding: 14px 32px;
    background: var(--brand-primary);
    color: #0f1117;
    font-size: var(--fs-small);
    font-weight: var(--fw-bold);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    transition: opacity 0.25s ease;
    white-space: nowrap;
  }

  .booking-widget__btn:hover {
    opacity: 0.88;
  }

  .booking-widget__placeholder {
    font-size: var(--fs-small);
    color: var(--text-muted);
    font-style: italic;
  }

  @media (max-width: 768px) {
    .booking-widget__inner {
      flex-direction: column;
      text-align: center;
    }

    .booking-widget__icon {
      display: none;
    }
  }
</style>


======================================================================
// REFERENCE: consultant-pro/components/widgets/NewsletterWidget.astro
======================================================================
---
// NewsletterWidget — dark theme, gold button
// POST to https://flowstarter.dev/api/leads/capture
---

<div class="newsletter-widget" data-project-id="{{PROJECT_ID}}">
  <div class="newsletter-widget__layout">
    <div class="newsletter-widget__text">
      <h3 class="newsletter-widget__title">Strategic insights, monthly.</h3>
      <p class="newsletter-widget__desc">Frameworks, case studies, and thinking on strategy and execution — direct to your inbox.</p>
    </div>
    <form class="newsletter-widget__form" data-newsletter-form novalidate>
      <div class="newsletter-widget__field-row">
        <input
          type="email"
          name="email"
          class="newsletter-widget__input"
          placeholder="your@email.com"
          required
          autocomplete="email"
        />
        <button type="submit" class="newsletter-widget__btn" data-newsletter-btn>
          <span class="newsletter-widget__btn-label">Subscribe</span>
          <span class="newsletter-widget__btn-loading" aria-hidden="true">...</span>
        </button>
      </div>
      <p class="newsletter-widget__error" data-newsletter-error aria-live="polite"></p>
    </form>
    <div class="newsletter-widget__success" data-newsletter-success aria-live="polite" hidden>
      <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
        <circle cx="10" cy="10" r="9" stroke="currentColor" stroke-width="1.5"/>
        <polyline points="6,10 9,13 14,7" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span>You're subscribed. Talk soon.</span>
    </div>
  </div>
</div>

<style>
  .newsletter-widget {
    background: var(--bg-card);
    border: 1px solid var(--border-gold);
    padding: 48px;
  }

  .newsletter-widget__layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 40px;
    align-items: center;
  }

  .newsletter-widget__title {
    font-family: var(--font-title);
    font-size: 1.3rem;
    font-weight: var(--fw-semibold);
    color: var(--text-primary);
    margin-bottom: 10px;
  }

  .newsletter-widget__desc {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .newsletter-widget__field-row {
    display: flex;
    gap: 0;
  }

  .newsletter-widget__input {
    flex: 1;
    background: var(--surface-field);
    border: 1px solid var(--surface-field-border);
    border-right: none;
    color: var(--text-primary);
    padding: 13px 16px;
    font-size: var(--fs-small);
    font-family: var(--font-body);
    outline: none;
    transition: border-color 0.2s ease;
    min-width: 0;
  }

  .newsletter-widget__input::placeholder {
    color: var(--text-muted);
  }

  .newsletter-widget__input:focus {
    border-color: var(--brand-primary);
    outline: 1px solid var(--brand-primary);
    outline-offset: -1px;
  }

  .newsletter-widget__btn {
    background: var(--brand-primary);
    color: #0f1117;
    border: 1px solid var(--brand-primary);
    padding: 13px 24px;
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    font-family: var(--font-button);
    cursor: pointer;
    transition: opacity 0.25s ease;
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .newsletter-widget__btn:hover {
    opacity: 0.88;
  }

  .newsletter-widget__btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  .newsletter-widget__btn-loading {
    display: none;
  }

  .newsletter-widget__btn.is-loading .newsletter-widget__btn-label {
    display: none;
  }

  .newsletter-widget__btn.is-loading .newsletter-widget__btn-loading {
    display: inline;
  }

  .newsletter-widget__error {
    font-size: var(--fs-xs);
    color: #e05c5c;
    margin-top: 8px;
    min-height: 1em;
  }

  .newsletter-widget__success {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--brand-primary);
    font-size: var(--fs-small);
    font-weight: var(--fw-medium);
    grid-column: 2;
  }

  .newsletter-widget__success[hidden] {
    display: none;
  }

  @media (max-width: 768px) {
    .newsletter-widget {
      padding: 32px 24px;
    }

    .newsletter-widget__layout {
      grid-template-columns: 1fr;
      gap: 24px;
    }

    .newsletter-widget__success {
      grid-column: 1;
    }
  }
</style>

<script>
  const widget = document.querySelector('.newsletter-widget') as HTMLElement;
  if (widget) {
    const form = widget.querySelector('[data-newsletter-form]') as HTMLFormElement;
    const btn = widget.querySelector('[data-newsletter-btn]') as HTMLButtonElement;
    const errorEl = widget.querySelector('[data-newsletter-error]') as HTMLElement;
    const successEl = widget.querySelector('[data-newsletter-success]') as HTMLElement;
    const projectId = widget.dataset.projectId || '';

    form?.addEventListener('submit', async (e) => {
      e.preventDefault();
      const email = (form.querySelector('input[name="email"]') as HTMLInputElement).value.trim();
      if (!email) {
        errorEl.textContent = 'Please enter a valid email address.';
        return;
      }

      btn.classList.add('is-loading');
      btn.disabled = true;
      errorEl.textContent = '';

      try {
        const res = await fetch('https://flowstarter.dev/api/leads/capture', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email, projectId, source: 'newsletter' }),
        });

        if (!res.ok) throw new Error('Submission failed');

        form.hidden = true;
        successEl.hidden = false;
      } catch {
        errorEl.textContent = 'Something went wrong. Please try again.';
        btn.classList.remove('is-loading');
        btn.disabled = false;
      }
    });
  }
</script>


======================================================================
// REFERENCE: consultant-pro/components/widgets/LeadCaptureWidget.astro
======================================================================
---
interface Props {
  title?: string;
  description?: string;
  className?: string;
}
const {
  title = "Send a Message",
  description = "Tell me about your situation. I'll reply within 24 hours.",
  className = '',
} = Astro.props;
---

<div class={`lead-capture-widget ${className}`} data-project-id="{{PROJECT_ID}}">
  <div class="lead-capture-widget__header">
    <h2 class="lead-capture-widget__title">{title}</h2>
    <p class="lead-capture-widget__desc">{description}</p>
  </div>

  <form class="lead-capture-widget__form" data-lead-form novalidate>
    <div class="lead-capture-widget__row">
      <div class="lead-capture-widget__field">
        <label class="lead-capture-widget__label" for="lcw-name">Full name</label>
        <input id="lcw-name" name="name" type="text" required
               placeholder="Your full name"
               class="lead-capture-widget__input" autocomplete="name" />
      </div>
      <div class="lead-capture-widget__field">
        <label class="lead-capture-widget__label" for="lcw-email">Email address</label>
        <input id="lcw-email" name="email" type="email" required
               placeholder="you@company.com"
               class="lead-capture-widget__input" autocomplete="email" />
      </div>
    </div>

    <div class="lead-capture-widget__field">
      <label class="lead-capture-widget__label" for="lcw-phone">Phone <span class="lead-capture-widget__optional">(optional)</span></label>
      <input id="lcw-phone" name="phone" type="tel"
             placeholder="+1 (555) 000-0000"
             class="lead-capture-widget__input" autocomplete="tel" />
    </div>

    <div class="lead-capture-widget__field">
      <label class="lead-capture-widget__label" for="lcw-message">Message</label>
      <textarea id="lcw-message" name="message" required
                placeholder="Tell me about your situation — what you're working on, the challenge you're facing, and what you're hoping to achieve."
                class="lead-capture-widget__textarea"
                rows="5"></textarea>
    </div>

    <div class="lead-capture-widget__footer">
      <button type="submit" class="lead-capture-widget__submit" data-lead-submit>
        Send Message
      </button>
      <p class="lead-capture-widget__success" hidden aria-live="polite">
        ✓ Message sent. I'll be in touch within 24 hours.
      </p>
      <p class="lead-capture-widget__error" hidden aria-live="polite">
        Something went wrong. Please try again or email directly.
      </p>
    </div>
  </form>
</div>

<script>
  document.querySelectorAll('[data-lead-form]').forEach(form => {
    form.addEventListener('submit', async e => {
      e.preventDefault();
      const widget = form.closest('[data-project-id]');
      const projectId = widget?.dataset.projectId ?? '';
      const btn = form.querySelector('[data-lead-submit]');
      const success = form.querySelector('.lead-capture-widget__success');
      const error = form.querySelector('.lead-capture-widget__error');
      const data = {
        projectId,
        name: form.querySelector('[name="name"]')?.value ?? '',
        email: form.querySelector('[name="email"]')?.value ?? '',
        phone: form.querySelector('[name="phone"]')?.value ?? '',
        message: form.querySelector('[name="message"]')?.value ?? '',
        source: 'contact',
      };
      const prev = btn.textContent;
      btn.disabled = true;
      btn.textContent = 'Sending…';
      success.hidden = true;
      error.hidden = true;
      try {
        const res = await fetch('https://flowstarter.dev/api/leads/capture', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
        });
        if (res.ok) {
          success.hidden = false;
          form.querySelectorAll('input, textarea').forEach(el => el.value = '');
        } else {
          error.hidden = false;
        }
      } catch {
        error.hidden = false;
      } finally {
        btn.disabled = false;
        btn.textContent = prev;
      }
    });
  });
</script>

<style>
  .lead-capture-widget {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    padding: 48px;
  }

  .lead-capture-widget__header {
    margin-bottom: 36px;
  }

  .lead-capture-widget__title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    color: var(--text-primary);
    margin-bottom: 10px;
    text-transform: uppercase;
    letter-spacing: -0.01em;
  }

  .lead-capture-widget__desc {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .lead-capture-widget__row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
  }

  .lead-capture-widget__field {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 20px;
  }

  .lead-capture-widget__label {
    font-size: var(--fs-small);
    font-weight: var(--fw-medium);
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.8px;
  }

  .lead-capture-widget__optional {
    color: var(--text-muted);
    font-weight: var(--fw-regular);
    text-transform: none;
    letter-spacing: 0;
  }

  .lead-capture-widget__input,
  .lead-capture-widget__textarea {
    background: var(--surface-field);
    border: 1px solid var(--surface-field-border);
    color: var(--text-primary);
    font-family: var(--font-body);
    font-size: var(--fs-body);
    padding: 12px 16px;
    width: 100%;
    transition: border-color 0.2s ease;
  }

  .lead-capture-widget__input::placeholder,
  .lead-capture-widget__textarea::placeholder {
    color: var(--text-muted);
  }

  .lead-capture-widget__input:focus,
  .lead-capture-widget__textarea:focus {
    outline: none;
    border-color: var(--brand-primary);
  }

  .lead-capture-widget__textarea {
    resize: vertical;
    min-height: 140px;
  }

  .lead-capture-widget__footer {
    margin-top: 8px;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .lead-capture-widget__submit {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 14px 36px;
    background: var(--brand-primary);
    color: #0f1117;
    font-family: var(--font-button);
    font-size: var(--fs-small);
    font-weight: var(--fw-bold);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    border: none;
    cursor: pointer;
    align-self: flex-start;
    transition: opacity 0.25s ease;
  }

  .lead-capture-widget__submit:hover {
    opacity: 0.88;
  }

  .lead-capture-widget__submit:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .lead-capture-widget__success {
    color: var(--brand-primary);
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
  }

  .lead-capture-widget__error {
    color: #e05c35;
    font-size: var(--fs-small);
  }

  @media (max-width: 768px) {
    .lead-capture-widget {
      padding: 32px 24px;
    }

    .lead-capture-widget__row {
      grid-template-columns: 1fr;
    }
  }
</style>


======================================================================
// REFERENCE: consultant-pro/layouts/Layout.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';

interface Props {
  title?: string;
}

const { siteMeta } = siteLabels;
const { title = siteMeta.title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={siteMeta.description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700;900&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;1,9..40,400&display=swap" rel="stylesheet" />
    <script is:inline>
      document.documentElement.classList.add('js');
    </script>
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>


======================================================================
// REFERENCE: consultant-pro/styles/global.css
======================================================================
/* Reset */
*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
  scrollbar-gutter: stable;
}

body {
  font-family: var(--font-body);
  background-color: var(--bg-dark);
  color: var(--text-primary);
  font-size: var(--fs-body);
  line-height: 1.3;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

img {
  max-width: 100%;
  display: block;
}

p {
  font-size: var(--fs-body);
  color: var(--text-primary);
}

a {
  color: inherit;
  text-decoration: none;
}

ul, ol {
  list-style: none;
}

button {
  font: inherit;
  cursor: pointer;
  border: none;
  background: none;
  border-radius: 0;
  font-family: var(--font-button);
}

/* Design Tokens */
:root {
  /* Colors */
  --bg-dark: #0f1117;
  --bg-darker: #080a0e;
  --bg-card: #181d28;
  --bg-card-hover: #1e2430;
  --brand-primary: #C9A84C;
  --brand-primary-dim: rgba(201,168,76,0.12);
  --surface-base: #0f1117;
  --surface-field: rgba(255,255,255,0.06);
  --surface-field-border: rgba(255,255,255,0.14);
  --surface-subtle: #181d28;
  --text-primary: #f0ece4;
  --text-on-dark: #f0ece4;
  --text-secondary: #8a8680;
  --text-muted: #5a5652;
  --border-color: rgba(255,255,255,0.08);
  --border-gold: rgba(201,168,76,0.25);

  /* Spacing */
  --section-padding: 100px 0;
  --container-width: 1200px;
  --container-padding: 0 24px;

  /* Typography */
  --fs-display: clamp(2.6rem, 5vw, 4rem);
  --fs-h1: clamp(2rem, 4vw, 3rem);
  --fs-h2: clamp(1.75rem, 3vw, 2.5rem);
  --fs-h3: 1.25rem;
  --fs-h4: 1.1rem;
  --fs-h5: 1rem;
  --fs-body: 1.05rem;
  --fs-small: 0.875rem;
  --fs-xs: 0.75rem;
  --font-title: 'Playfair Display', Georgia, serif;
  --font-body: 'DM Sans', sans-serif;
  --font-button: 'DM Sans', sans-serif;

  --fw-regular: 400;
  --fw-medium: 500;
  --fw-semibold: 600;
  --fw-bold: 700;
  --fw-black: 900;
  --fw-headline: 700;

  /* Transitions */
  --transition: 0.3s ease;
}

/* Utilities */
.container {
  max-width: var(--container-width);
  margin: 0 auto;
  padding: var(--container-padding);
}

.section-label {
  font-size: var(--fs-xs);
  text-transform: uppercase;
  letter-spacing: 3px;
  color: var(--brand-primary);
  margin-bottom: 16px;
  font-family: var(--font-body);
  font-weight: var(--fw-medium);
}

.section-title {
  font-size: var(--fs-h2);
  font-family: var(--font-title);
  font-weight: var(--fw-bold);
  text-transform: uppercase;
  line-height: 1.1;
  letter-spacing: -0.01em;
  color: var(--text-primary);
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-title);
  color: var(--text-primary);
}

h1 { font-size: var(--fs-h1); font-weight: var(--fw-bold); }
h2 { font-size: var(--fs-h2); font-weight: var(--fw-bold); }
h3 { font-size: var(--fs-h3); font-weight: var(--fw-semibold); }
h4 { font-size: var(--fs-h4); font-weight: var(--fw-semibold); }
h5 { font-size: var(--fs-h5); font-weight: var(--fw-medium); }

.ds-action-button {
  font-family: var(--font-button);
}

/* Responsive */
@media (max-width: 768px) {
  :root {
    --section-padding: 64px 0;
    --container-padding: 0 20px;
  }
}


======================================================================
// REFERENCE: consultant-pro/pages/index.astro
======================================================================
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import Services from '../components/Services.astro';
import Process from '../components/Process.astro';
import Testimonials from '../components/Testimonials.astro';
import CTA from '../components/CTA.astro';
import Footer from '../components/Footer.astro';
import { frontmatter as siteLabels } from '../content/site-labels.md';

const { stats } = siteLabels;
---

<Layout>
  <Header activeIndex={0} />
  <main>
    <Hero />

    <!-- Stats bar -->
    <div class="stats-bar">
      <div class="container">
        <div class="stats-bar__inner">
          {stats.items.map((s) => (
            <div class="stat-item">
              <span class="stat-item__number">{s.number}</span>
              <span class="stat-item__label">{s.label}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <Services />
    <Process />
    <Testimonials />
    <CTA />
  </main>
  <Footer />
</Layout>

<style>
  .stats-bar {
    background: var(--bg-card);
    border-bottom: 1px solid var(--border-color);
  }

  .stats-bar__inner {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
  }

  .stat-item {
    padding: 28px 32px;
    border-right: 1px solid var(--border-color);
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .stat-item:last-child {
    border-right: none;
  }

  .stat-item__number {
    font-family: var(--font-title);
    font-size: clamp(1.6rem, 2.5vw, 2.2rem);
    font-weight: var(--fw-headline);
    color: var(--brand-primary);
    line-height: 1;
  }

  .stat-item__label {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-medium);
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: var(--text-secondary);
  }

  @media (max-width: 768px) {
    .stats-bar__inner {
      grid-template-columns: repeat(2, 1fr);
    }

    .stat-item {
      padding: 20px 24px;
      border-bottom: 1px solid var(--border-color);
    }

    .stat-item:nth-child(even) {
      border-right: none;
    }
  }
</style>


======================================================================
// REFERENCE: consultant-pro/pages/about.astro
======================================================================
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SectionBlock from '../components/design-system/SectionBlock.astro';
import ActionButton from '../components/design-system/ActionButton.astro';
import { frontmatter as siteLabels } from '../content/site-labels.md';

const { siteMeta } = siteLabels;
const base = import.meta.env.BASE_URL;
---

<Layout title={`About — ${siteMeta.title}`}>
  <Header activeIndex={1} />
  <main>
    <!-- Page Hero -->
    <div class="page-hero">
      <div class="container">
        <p class="page-hero__eyebrow">About</p>
        <h1 class="page-hero__title">BUILT ON RESULTS,<br />NOT REPORTS</h1>
        <p class="page-hero__sub">I'm an independent strategy consultant with a decade of experience helping founders and leadership teams make better decisions, faster.</p>
      </div>
    </div>

    <!-- Story -->
    <SectionBlock tone="light" padding="default" topBorder={true} className="about-story">
      <div class="container">
        <div class="about-story__grid">
          <div class="about-story__content">
            <span class="gold-rule"></span>
            <h2 class="about-story__title">Why I work independently</h2>
            <p>Large consultancies are built for large clients. Their frameworks are designed to justify big fees through thick decks, large teams, and multi-year engagements.</p>
            <p>I work differently. My clients get direct access to senior thinking — no juniors running the project, no overhead to justify. Just focused strategic work that moves fast and delivers results you can see.</p>
            <p>I've spent my career inside high-growth companies, helping them navigate inflection points: new markets, leadership transitions, competitive threats, and expansion decisions. I bring that operational experience to every engagement.</p>
            <div class="about-story__cta">
              <ActionButton href={`${base}contact`} label="Work Together" variant="primary" />
            </div>
          </div>
          <div class="about-story__visual" aria-hidden="true">
            <div class="about-visual__card">
              <div class="about-visual__number">10</div>
              <div class="about-visual__label">Years consulting</div>
            </div>
            <div class="about-visual__card">
              <div class="about-visual__number">50+</div>
              <div class="about-visual__label">Client engagements</div>
            </div>
            <div class="about-visual__card about-visual__card--accent">
              <div class="about-visual__quote">"The best strategy is one that actually gets executed."</div>
            </div>
          </div>
        </div>
      </div>
    </SectionBlock>

    <!-- Values -->
    <SectionBlock tone="dark" padding="default" className="about-values">
      <div class="container">
        <p class="section-label">How I Think</p>
        <h2 class="section-title" style="margin-bottom: 48px;">MY PRINCIPLES</h2>
        <div class="values__grid">
          {[
            { title: "Clarity is the product", desc: "Every engagement produces clarity — about your situation, your options, and your path forward. That clarity is what you're paying for." },
            { title: "Speed matters", desc: "Slow strategy is bad strategy. I work fast and direct. You'll have a clear output within days, not months." },
            { title: "No pretending", desc: "I don't know everything. But I know how to ask the right questions, find the right answers, and tell you what I actually think." },
            { title: "Skin in the game", desc: "I price my work partly on outcomes, not just effort. When you win, I win. That's the only way I like to operate." },
          ].map((v) => (
            <div class="value-card">
              <h3 class="value-card__title">{v.title}</h3>
              <p class="value-card__desc">{v.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </SectionBlock>
  </main>
  <Footer />
</Layout>

<style>
  .page-hero {
    background: var(--bg-dark);
    padding: 140px 0 80px;
    border-bottom: 1px solid var(--border-gold);
  }

  .page-hero__eyebrow {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--brand-primary);
    margin-bottom: 20px;
  }

  .page-hero__title {
    font-family: var(--font-title);
    font-size: var(--fs-display);
    font-weight: var(--fw-headline);
    color: var(--text-on-dark);
    line-height: 1.1;
    margin-bottom: 24px;
    letter-spacing: -0.01em;
  }

  .page-hero__sub {
    font-size: var(--fs-h5);
    color: var(--text-secondary);
    max-width: 580px;
    line-height: 1.65;
  }

  :global(.about-story) {
    background: var(--bg-dark);
  }

  .about-story__grid {
    display: grid;
    grid-template-columns: 1fr 360px;
    gap: 80px;
    align-items: start;
  }

  .about-story__content {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .about-story__title {
    font-family: var(--font-title);
    font-size: var(--fs-h2);
    font-weight: var(--fw-headline);
    color: var(--text-on-dark);
    margin-bottom: 8px;
  }

  .about-story__content p {
    color: var(--text-secondary);
    line-height: 1.7;
  }

  .about-story__cta {
    margin-top: 8px;
  }

  .about-story__visual {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .about-visual__card {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    padding: 28px 24px;
  }

  .about-visual__card--accent {
    border-color: var(--border-gold);
  }

  .about-visual__number {
    font-family: var(--font-title);
    font-size: 3rem;
    font-weight: var(--fw-headline);
    color: var(--brand-primary);
    line-height: 1;
    margin-bottom: 6px;
  }

  .about-visual__label {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 1.5px;
  }

  .about-visual__quote {
    font-family: var(--font-title);
    font-size: 1.05rem;
    font-style: italic;
    color: var(--brand-primary);
    line-height: 1.5;
  }

  :global(.about-values) {
    background: var(--bg-darker);
  }

  .values__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0;
    border-top: 1px solid var(--border-color);
    border-left: 1px solid var(--border-color);
  }

  .value-card {
    padding: 36px 32px;
    border-right: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
  }

  .value-card__title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    font-weight: var(--fw-bold);
    color: var(--text-on-dark);
    margin-bottom: 12px;
  }

  .value-card__desc {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.7;
  }

  @media (max-width: 900px) {
    .about-story__grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }

    .about-story__visual {
      flex-direction: row;
      flex-wrap: wrap;
    }

    .about-visual__card {
      flex: 1;
      min-width: 140px;
    }

    .about-visual__card--accent {
      flex: 100%;
    }
  }

  @media (max-width: 640px) {
    .values__grid {
      grid-template-columns: 1fr;
    }
  }
</style>


======================================================================
// REFERENCE: consultant-pro/pages/services.astro
======================================================================
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Services from '../components/Services.astro';
import Process from '../components/Process.astro';
import CTA from '../components/CTA.astro';
import SectionBlock from '../components/design-system/SectionBlock.astro';
import { frontmatter as siteLabels } from '../content/site-labels.md';

const { siteMeta } = siteLabels;
---

<Layout title={`Services — ${siteMeta.title}`}>
  <Header activeIndex={2} />
  <main>
    <div class="page-hero">
      <div class="container">
        <p class="page-hero__eyebrow">Services</p>
        <h1 class="page-hero__title">ENGAGEMENTS BUILT<br />FOR RESULTS</h1>
        <p class="page-hero__sub">Every engagement is designed to produce a clear output in a defined timeframe. No ambiguity, no scope creep — just focused strategic work.</p>
      </div>
    </div>

    <Services />
    <Process />

    <!-- FAQ-style section -->
    <SectionBlock tone="light" padding="default" topBorder={true} className="how-it-works">
      <div class="container">
        <p class="section-label">How It Works</p>
        <h2 class="section-title" style="margin-bottom: 48px;">WHAT TO EXPECT</h2>
        <div class="hiw__steps">
          {[
            { n: "1", title: "Book a discovery call", desc: "A 30-minute conversation to understand your situation. No pitch, no pressure — just an honest assessment of whether we're a fit." },
            { n: "2", title: "Proposal within 48 hours", desc: "If there's a clear way I can help, you'll receive a scoped proposal with a fixed price, clear timeline, and defined output." },
            { n: "3", title: "Engagement begins", desc: "We kick off with a deep-dive session. I get into the data, the context, and the constraints before recommending anything." },
            { n: "4", title: "Output delivered", desc: "You receive a clear, actionable deliverable — a strategy document, decision framework, or workshop output — within the agreed timeline." },
          ].map((s) => (
            <div class="hiw-step">
              <div class="hiw-step__num">{s.n}</div>
              <div class="hiw-step__content">
                <h3 class="hiw-step__title">{s.title}</h3>
                <p class="hiw-step__desc">{s.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </SectionBlock>

    <CTA />
  </main>
  <Footer />
</Layout>

<style>
  .page-hero {
    background: var(--bg-dark);
    padding: 140px 0 80px;
    border-bottom: 1px solid var(--border-gold);
  }

  .page-hero__eyebrow {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--brand-primary);
    margin-bottom: 20px;
  }

  .page-hero__title {
    font-family: var(--font-title);
    font-size: var(--fs-display);
    font-weight: var(--fw-headline);
    color: var(--text-on-dark);
    line-height: 1.1;
    margin-bottom: 24px;
    letter-spacing: -0.01em;
  }

  .page-hero__sub {
    font-size: var(--fs-h5);
    color: var(--text-secondary);
    max-width: 540px;
    line-height: 1.65;
  }

  :global(.how-it-works) {
    background: var(--bg-dark);
  }

  .hiw__steps {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .hiw-step {
    display: grid;
    grid-template-columns: 56px 1fr;
    gap: 0 32px;
    padding: 32px 0;
    border-bottom: 1px solid var(--border-color);
    align-items: start;
  }

  .hiw-step:first-child {
    border-top: 1px solid var(--border-color);
  }

  .hiw-step__num {
    font-family: var(--font-title);
    font-size: 2rem;
    font-weight: var(--fw-headline);
    color: var(--brand-primary);
    line-height: 1;
    padding-top: 2px;
  }

  .hiw-step__title {
    font-family: var(--font-title);
    font-size: var(--fs-h4);
    font-weight: var(--fw-bold);
    color: var(--text-on-dark);
    margin-bottom: 8px;
  }

  .hiw-step__desc {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    line-height: 1.65;
  }
</style>


======================================================================
// REFERENCE: consultant-pro/pages/contact.astro
======================================================================
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SectionBlock from '../components/design-system/SectionBlock.astro';
import BookingWidget from '../components/widgets/BookingWidget.astro';
import LeadCaptureWidget from '../components/widgets/LeadCaptureWidget.astro';
import { frontmatter as siteLabels } from '../content/site-labels.md';

const { siteMeta } = siteLabels;
---

<Layout title={`Contact — ${siteMeta.title}`}>
  <Header activeIndex={3} />
  <main>
    <div class="page-hero">
      <div class="container">
        <p class="page-hero__eyebrow">Contact</p>
        <h1 class="page-hero__title">LET'S TALK STRATEGY</h1>
        <p class="page-hero__sub">Book a discovery call or send a message. I respond within 24 hours on business days.</p>
      </div>
    </div>

    <SectionBlock tone="light" padding="default" className="contact-section">
      <div class="container">
        <div class="contact__grid">
          <div class="contact__info">
            <span class="gold-rule"></span>
            <h2 class="contact__info-title">What happens next</h2>
            <div class="contact__steps">
              {[
                "You fill in the form with a brief description of your challenge.",
                "I review it and respond within 24 hours with availability.",
                "We have a 30-minute discovery call — no obligation.",
                "If there's a fit, you receive a scoped proposal within 48 hours.",
              ].map((step, i) => (
                <div class="contact-step">
                  <span class="contact-step__n">{i + 1}</span>
                  <p class="contact-step__text">{step}</p>
                </div>
              ))}
            </div>
          </div>

          <div class="contact__form-wrap">
            <LeadCaptureWidget title="Send a Message" description="Tell me about your situation. I'll reply within 24 hours." />
          </div>
        </div>
      </div>
    </SectionBlock>
  </main>
  <Footer />
</Layout>

<style>
  .page-hero {
    background: var(--bg-dark);
    padding: 140px 0 80px;
    border-bottom: 1px solid var(--border-gold);
  }

  .page-hero__eyebrow {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--brand-primary);
    margin-bottom: 20px;
  }

  .page-hero__title {
    font-family: var(--font-title);
    font-size: var(--fs-display);
    font-weight: var(--fw-headline);
    color: var(--text-on-dark);
    line-height: 1.1;
    margin-bottom: 24px;
    letter-spacing: -0.01em;
  }

  .page-hero__sub {
    font-size: var(--fs-h5);
    color: var(--text-secondary);
    max-width: 480px;
    line-height: 1.65;
  }

  :global(.contact-section) {
    background: var(--bg-dark);
  }

  .contact__grid {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 80px;
    align-items: start;
  }

  .contact__info-title {
    font-family: var(--font-title);
    font-size: var(--fs-h2);
    font-weight: var(--fw-headline);
    color: var(--text-on-dark);
    margin-bottom: 32px;
  }

  .contact__steps {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .contact-step {
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }

  .contact-step__n {
    width: 28px;
    height: 28px;
    background: var(--brand-primary);
    color: var(--bg-dark);
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-bold);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .contact-step__text {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.6;
    padding-top: 4px;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 24px;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    padding: 40px;
  }

  .form-field {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .form-label {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: var(--text-secondary);
  }

  .form-input,
  .form-textarea {
    background: var(--bg-dark);
    border: 1px solid var(--border-color);
    color: var(--text-on-dark);
    font-family: var(--font-body);
    font-size: var(--fs-body);
    padding: 12px 16px;
    border-radius: 0;
    outline: none;
    transition: border-color var(--transition);
    width: 100%;
    resize: vertical;
  }

  .form-input:focus,
  .form-textarea:focus {
    border-color: var(--brand-primary);
  }

  .form-input::placeholder,
  .form-textarea::placeholder {
    color: var(--text-muted);
  }

  .form-submit {
    font-family: var(--font-button);
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 1.5px;
    background: var(--brand-primary);
    color: var(--bg-dark);
    border: none;
    padding: 16px 32px;
    cursor: pointer;
    transition: background var(--transition);
    align-self: flex-start;
  }

  .form-submit:hover {
    background: #d4b560;
  }

  @media (max-width: 900px) {
    .contact__grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }

    .contact-form {
      padding: 28px 24px;
    }
  }
</style>


======================================================================
// REFERENCE: consultant-pro/scripts/useVisibilityClass.js
======================================================================
export function useVisibilityClass({
  selector,
  className = 'is-visible',
  observingClassName = 'is-observing',
  threshold = 0.35,
  rootMargin = '0px',
  once = false,
  respectReducedMotion = true,
  revealIfAlreadyVisible = true,
}) {
  const elements = Array.from(document.querySelectorAll(selector));

  if (!elements.length) {
    return;
  }

  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (respectReducedMotion && reduceMotion) {
    elements.forEach((element) => {
      element.classList.add(observingClassName);
      element.classList.add(className);
    });
    return;
  }

  const isInViewport = (element) => {
    const rect = element.getBoundingClientRect();
    return rect.bottom > 0 && rect.top < window.innerHeight;
  };

  const revealOnNextFrame = (element) => {
    if (element.dataset.revealQueued === 'true') {
      return;
    }

    element.dataset.revealQueued = 'true';

    window.requestAnimationFrame(() => {
      window.requestAnimationFrame(() => {
        element.classList.add(className);
        delete element.dataset.revealQueued;
      });
    });
  };

  elements.forEach((element) => {
    element.classList.add(observingClassName);

    if (revealIfAlreadyVisible && isInViewport(element)) {
      revealOnNextFrame(element);
    }
  });

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add(className);

          if (once) {
            observer.unobserve(entry.target);
          }

          return;
        }

        if (!once) {
          entry.target.classList.remove(className);
        }
      });
    },
    {
      threshold,
      rootMargin,
    }
  );

  elements.forEach((element) => observer.observe(element));
}


======================================================================
// REFERENCE: consultant-pro/content/site-labels.md
======================================================================
---
siteMeta:
  title: "Your Name — Independent Strategy Consultant"
  description: "Independent strategy consulting for founders and leadership teams who need clarity, direction, and results — without the overhead."
header:
  logo: "Your Name"
  navLinks:
    - label: "Home"
      href: "/"
    - label: "About"
      href: "/about"
    - label: "Services"
      href: "/services"
    - label: "Contact"
      href: "/contact"
hero:
  eyebrow: "Independent Strategy Consultant"
  title: "CLARITY IS THE STRATEGY"
  subtitle: "For founders and leadership teams who need decisive thinking, sharp execution, and results that compound — without the overhead of a large consultancy."
  cta_primary: "Book a Discovery Call"
  cta_secondary: "See My Services"
services:
  sectionLabel: "Engagements"
  sectionTitle: "HOW I WORK"
  items:
    - title: "Strategy Sprint"
      description: "2-week intensive. Diagnose the core problem, build a 90-day action plan. Fast, focused, ready to execute."
    - title: "Fractional Advisor"
      description: "Monthly retainer for ongoing strategic guidance and decision support. Senior expertise at a fraction of the cost."
    - title: "Board Advisory"
      description: "Quarterly engagement for governance support, strategic oversight, and accountability at the leadership level."
    - title: "Team Alignment"
      description: "Workshop-based engagement to align leadership teams on priorities, clear bottlenecks, and create shared direction."
    - title: "Market Entry"
      description: "Market entry strategy, positioning, channel selection, and launch planning for new products or expansions."
    - title: "Executive Coaching"
      description: "1:1 coaching for senior leaders navigating high-stakes decisions, transitions, or significant scale challenges."
process:
  sectionLabel: "My Approach"
  sectionTitle: "HOW GREAT STRATEGY GETS DONE"
  items:
    - number: "01"
      title: "Diagnose before prescribing"
      description: "Most consultants arrive with pre-built solutions. I start by understanding your specific situation deeply before recommending anything."
    - number: "02"
      title: "Clarity over volume"
      description: "A 5-page strategy document that gets executed beats a 50-page deck that sits on a shelf. I write for action, not for appearance."
    - number: "03"
      title: "Speed as a feature"
      description: "Every engagement has a clear output and a deadline. Ambiguity is the enemy of momentum."
    - number: "04"
      title: "Accountability built in"
      description: "I stay involved through execution. Strategy without follow-through is just an opinion."
    - number: "05"
      title: "Results you can measure"
      description: "Every engagement defines success upfront. At the end, you should know clearly whether it worked."
testimonials:
  - quote: "We were stuck in analysis paralysis for months. One session cut through it all. We had a decision framework by lunch."
    authorName: "Alex M."
    authorRole: "CEO, Series A SaaS"
  - quote: "The strategy sprint paid for itself in the first month. The plan was sharp, the execution was clear."
    authorName: "Elena V."
    authorRole: "Founder, E-commerce Brand"
  - quote: "Finally someone who gives direct answers rather than more questions. Rare in this industry."
    authorName: "David K."
    authorRole: "COO, Scale-up"
  - quote: "Doubled our qualified pipeline in 90 days by getting clear on who we actually serve and why."
    authorName: "Rachel T."
    authorRole: "Managing Director, B2B Services"
stats:
  items:
    - number: "50+"
      label: "Engagements Delivered"
    - number: "$5M+"
      label: "Client Revenue Impact"
    - number: "10"
      label: "Years Experience"
    - number: "4.9★"
      label: "Client Rating"
cta:
  title: "Ready to build a strategy that actually gets executed?"
  description: "Let's talk about where you are and where you need to go. No pitch — just a clear conversation."
  primary: "Book a Discovery Call"
  secondary: "View Services"
footer:
  logo: "Your Name"
  tagline: "Independent strategy consulting for founders and leadership teams who are serious about execution."
  copyrightSuffix: "Your Name Strategy. All rights reserved."
aboutPage:
  heroTitle: "I HELP FOUNDERS AND LEADERS TURN COMPLEXITY INTO CLARITY"
  intro:
    - "I've spent a decade working inside and alongside ambitious companies — from early-stage startups to mid-market businesses navigating complex growth."
    - "My work sits at the intersection of strategy and execution. I help leaders make better decisions faster, build aligned teams, and create the conditions for sustainable growth."
  experience:
    - "Worked across technology, professional services, and consumer businesses"
    - "Led strategy projects for companies ranging from seed-stage to £50M+ revenue"
    - "Built and managed teams, run P&Ls, and sat in the room when the hard calls were made"
    - "Independent — no agenda beyond getting you the right answer"
  faq:
    - question: "What size companies do you typically work with?"
      answer: "I work best with founder-led businesses and leadership teams at Series A through Series C stage, and with established businesses ($5M–$50M revenue) navigating a specific strategic challenge."
    - question: "How long does a typical engagement last?"
      answer: "Strategy Sprints are 2 weeks. Fractional Advisor engagements typically run 3–6 months. I prefer defined engagements with clear outcomes over open-ended retainers."
    - question: "Do you work remotely?"
      answer: "Yes — entirely. I work with clients across Europe and North America over video calls and async tools. Occasional in-person intensives are possible for the right engagements."
    - question: "How do you charge?"
      answer: "Fixed-fee engagements for Strategy Sprints. Monthly retainers for Fractional Advisor and Board Advisory roles. I don't charge by the hour — I charge for outcomes."
    - question: "What industries do you focus on?"
      answer: "My strongest experience is in technology, professional services, and B2B businesses. I've also worked in consumer, media, and fintech. The methodology is industry-agnostic."
servicesPage:
  heroTitle: "STRATEGY THAT MOVES BUSINESSES FORWARD"
  description: "Focused engagements with clear outcomes. No retainer lock-in, no surprise fees."
  faq:
    - question: "What's included in a Strategy Sprint?"
      answer: "A 2-week intensive covering: kick-off diagnostic, problem framing, root cause analysis, 90-day action plan, final readout session, and one 30-day follow-up call."
    - question: "Can I start with a single session?"
      answer: "Yes — I offer a 90-minute diagnostic session to assess whether a longer engagement makes sense. This is a good first step if you're unsure of the scope."
    - question: "Do you sign NDAs?"
      answer: "Yes, always. Everything shared during an engagement is treated with full confidentiality."
contactPage:
  heroTitle: "LET'S TALK ABOUT YOUR STRATEGY"
  intro: "No sales pitch. Just a direct conversation about where you are and how I can help."
  email: "hello@example.com"
  phone: "+1 (555) 000-0000"
  location: "Available remotely worldwide"
---


======================================================================
// REFERENCE: coach-pro/components/Header.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
const { header } = siteLabels;
const base = import.meta.env.BASE_URL;
const currentPath = Astro.url.pathname;
---

<header class="header">
  <div class="header__inner container">
    <a href={base} class="header__logo">{header.logo}</a>

    <nav class="header__nav" aria-label="Main navigation">
      <ul class="header__nav-list">
        {header.navLinks.map((link: { label: string; href: string }) => {
          const fullHref = link.href === '/' ? base : `${base}${link.href.replace(/^\//, '')}`;
          const isActive = currentPath === fullHref || (link.href !== '/' && currentPath.startsWith(fullHref));
          return (
            <li>
              <a
                href={fullHref}
                class={`header__nav-link ${isActive ? 'header__nav-link--active' : ''}`}
              >
                {link.label}
              </a>
            </li>
          );
        })}
      </ul>
    </nav>

    <a href={`${base}contact`} class="header__cta">Book a Call</a>

    <button class="header__burger" aria-label="Open menu" aria-expanded="false" aria-controls="mobile-menu">
      <span></span>
      <span></span>
      <span></span>
    </button>
  </div>

  <div class="header__mobile-menu" id="mobile-menu" aria-hidden="true">
    <ul class="header__mobile-list">
      {header.navLinks.map((link: { label: string; href: string }) => {
        const fullHref = link.href === '/' ? base : `${base}${link.href.replace(/^\//, '')}`;
        return (
          <li>
            <a href={fullHref} class="header__mobile-link">{link.label}</a>
          </li>
        );
      })}
      <li>
        <a href={`${base}contact`} class="header__mobile-cta">Book a Free Call</a>
      </li>
    </ul>
  </div>
</header>

<script>
  const burger = document.querySelector('.header__burger') as HTMLButtonElement;
  const mobileMenu = document.getElementById('mobile-menu');
  const header = document.querySelector('.header') as HTMLElement;

  burger?.addEventListener('click', () => {
    const isOpen = burger.getAttribute('aria-expanded') === 'true';
    burger.setAttribute('aria-expanded', String(!isOpen));
    mobileMenu?.setAttribute('aria-hidden', String(isOpen));
    burger.classList.toggle('is-open');
    mobileMenu?.classList.toggle('is-open');
  });

  // Close on link click
  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
      burger?.setAttribute('aria-expanded', 'false');
      mobileMenu?.setAttribute('aria-hidden', 'true');
      burger?.classList.remove('is-open');
      mobileMenu?.classList.remove('is-open');
    });
  });

  // Scroll behavior
  let lastScrollY = 0;
  window.addEventListener('scroll', () => {
    const scrollY = window.scrollY;
    if (scrollY > 60) {
      header?.classList.add('header--scrolled');
    } else {
      header?.classList.remove('header--scrolled');
    }
    lastScrollY = scrollY;
  });
</script>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    transition: background-color 0.3s ease, box-shadow 0.3s ease;
  }

  .header--scrolled {
    background: rgba(253, 248, 240, 0.96);
    box-shadow: 0 2px 20px rgba(26, 14, 8, 0.08);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  .header__inner {
    display: flex;
    align-items: center;
    gap: 40px;
    height: 72px;
  }

  .header__logo {
    font-family: var(--font-title);
    font-size: 1.15rem;
    font-weight: 700;
    color: var(--text-primary);
    letter-spacing: -0.01em;
    flex-shrink: 0;
    margin-right: auto;
  }

  .header__nav {
    display: flex;
  }

  .header__nav-list {
    display: flex;
    align-items: center;
    gap: 36px;
  }

  .header__nav-link {
    font-size: var(--fs-small);
    font-weight: 500;
    color: var(--text-secondary);
    letter-spacing: 0.2px;
    transition: color var(--transition);
    position: relative;
  }

  .header__nav-link::after {
    content: '';
    position: absolute;
    bottom: -3px;
    left: 0;
    right: 0;
    height: 1.5px;
    background: var(--brand-primary);
    transform: scaleX(0);
    transition: transform var(--transition);
    transform-origin: left;
  }

  .header__nav-link:hover,
  .header__nav-link--active {
    color: var(--text-primary);
  }

  .header__nav-link:hover::after,
  .header__nav-link--active::after {
    transform: scaleX(1);
  }

  .header__cta {
    font-size: var(--fs-small);
    font-weight: 600;
    color: var(--brand-primary);
    border: 1.5px solid var(--brand-primary);
    padding: 8px 20px;
    border-radius: var(--radius-sm);
    transition: background-color var(--transition), color var(--transition);
    flex-shrink: 0;
  }

  .header__cta:hover {
    background: var(--brand-primary);
    color: #fff;
  }

  .header__burger {
    display: none;
    flex-direction: column;
    gap: 5px;
    width: 28px;
    padding: 4px 0;
    cursor: pointer;
    flex-shrink: 0;
  }

  .header__burger span {
    display: block;
    height: 2px;
    background: var(--text-primary);
    border-radius: 2px;
    transition: transform 0.25s ease, opacity 0.25s ease;
  }

  .header__burger.is-open span:nth-child(1) {
    transform: translateY(7px) rotate(45deg);
  }

  .header__burger.is-open span:nth-child(2) {
    opacity: 0;
  }

  .header__burger.is-open span:nth-child(3) {
    transform: translateY(-7px) rotate(-45deg);
  }

  .header__mobile-menu {
    display: none;
    background: var(--surface-base);
    border-top: 1px solid var(--border-color);
    padding: 24px 0 32px;
  }

  .header__mobile-menu.is-open {
    display: block;
  }

  .header__mobile-list {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: 0 24px;
  }

  .header__mobile-link {
    display: block;
    padding: 12px 0;
    font-size: 1.05rem;
    font-weight: 500;
    color: var(--text-primary);
    border-bottom: 1px solid var(--border-color);
  }

  .header__mobile-cta {
    display: inline-flex;
    margin-top: 20px;
    padding: 12px 28px;
    background: var(--brand-primary);
    color: #fff;
    font-size: var(--fs-small);
    font-weight: 600;
    border-radius: var(--radius-sm);
  }

  @media (max-width: 768px) {
    .header__nav,
    .header__cta {
      display: none;
    }

    .header__burger {
      display: flex;
    }

    .header--scrolled .header__mobile-menu.is-open {
      box-shadow: 0 8px 24px rgba(26, 14, 8, 0.1);
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/Hero.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import ActionButton from './design-system/ActionButton.astro';
const { hero } = siteLabels;
const base = import.meta.env.BASE_URL;
---

<section class="hero">
  <div class="hero__inner container">
    <div class="hero__content">
      <p class="hero__eyebrow">{hero.eyebrow}</p>
      <h1 class="hero__title" set:html={hero.title_html} />
      <p class="hero__subtitle">{hero.subtitle}</p>
      <p class="hero__social-proof">{hero.social_proof}</p>
      <div class="hero__actions">
        <ActionButton href={`${base}contact`} label={hero.cta_primary} variant="primary" />
        <ActionButton href={`${base}services`} label={hero.cta_secondary} variant="outline" />
      </div>
    </div>

    <div class="hero__visual" aria-hidden="true">
      <div class="hero__ring-wrap">
        <svg class="hero__ring-svg" viewBox="0 0 300 300" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <path id="ring-path" d="M150,150 m-110,0 a110,110 0 1,1 220,0 a110,110 0 1,1 -220,0" />
            <radialGradient id="ring-fill" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stop-color="#E05C35" stop-opacity="0.18" />
              <stop offset="100%" stop-color="#E05C35" stop-opacity="0.04" />
            </radialGradient>
          </defs>
          <circle cx="150" cy="150" r="110" fill="none" stroke="#E05C35" stroke-width="1" opacity="0.2" />
          <circle cx="150" cy="150" r="80" fill="url(#ring-fill)" />
          <circle cx="150" cy="150" r="80" fill="none" stroke="#E05C35" stroke-width="0.5" opacity="0.35" />
          <text class="hero__ring-text">
            <textPath href="#ring-path" startOffset="0%">
              TRANSFORM · GROW · ACHIEVE · LEAD · TRANSFORM · GROW · ACHIEVE · LEAD ·
            </textPath>
          </text>
        </svg>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    background: var(--surface-base);
    display: flex;
    align-items: center;
    overflow: hidden;
    position: relative;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 72% 40%, rgba(224, 92, 53, 0.07) 0%, transparent 60%);
    pointer-events: none;
  }

  .hero__inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
    padding: 120px 0 80px;
  }

  .hero__eyebrow {
    font-family: var(--font-body);
    font-size: var(--fs-small);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 2.5px;
    color: var(--brand-primary);
    margin-bottom: 20px;
  }

  .hero__title {
    font-family: var(--font-title);
    font-size: var(--fs-display);
    font-weight: 700;
    line-height: 1.05;
    letter-spacing: -0.02em;
    margin-bottom: 24px;
    color: var(--text-primary);
  }

  .hero__title :global(em) {
    color: var(--brand-primary);
    font-style: italic;
  }

  .hero__subtitle {
    font-size: 1.1rem;
    line-height: 1.7;
    color: var(--text-secondary);
    max-width: 480px;
    margin-bottom: 20px;
  }

  .hero__social-proof {
    font-size: var(--fs-small);
    color: var(--text-muted);
    margin-bottom: 36px;
    letter-spacing: 0.3px;
  }

  .hero__actions {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
  }

  /* Ring visual */
  .hero__visual {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero__ring-wrap {
    animation: ring-rotate 24s linear infinite;
    width: 100%;
    max-width: 400px;
  }

  @keyframes ring-rotate {
    to {
      transform: rotate(360deg);
    }
  }

  .hero__ring-svg {
    width: 100%;
    height: auto;
  }

  .hero__ring-text {
    font-family: var(--font-body);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 3px;
    fill: var(--brand-primary);
    text-transform: uppercase;
  }

  @media (max-width: 1024px) {
    .hero__inner {
      grid-template-columns: 1fr;
      padding: 120px 0 72px;
    }

    .hero__visual {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .hero__inner {
      padding: 100px 0 64px;
    }

    .hero__subtitle {
      max-width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__ring-wrap {
      animation: none;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/Services.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import SectionBlock from './design-system/SectionBlock.astro';
import SectionHeading from './design-system/SectionHeading.astro';

const services = siteLabels.programs || siteLabels.services;
---

<SectionBlock id="services" tone="warm" padding="default" className="services">
  <div class="container">
    <SectionHeading label={services.sectionLabel} title={services.sectionTitle} align="center" />

    <div class="services__grid">
      {services.items.map((item, i) => (
        <article class="service-card">
          <div class="service-card__accent"></div>
          <div class="service-card__content">
            <span class="service-card__num">{String(i + 1).padStart(2, '0')}</span>
            <h3 class="service-card__title">{item.title}</h3>
            <p class="service-card__desc">{item.description}</p>
            <span class="service-card__link">Learn more →</span>
          </div>
        </article>
      ))}
    </div>
  </div>
</SectionBlock>

<style>
  :global(.services) {
    background: var(--surface-warm);
  }

  .services__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  .service-card {
    background: var(--surface-card);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-color);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    box-shadow: var(--shadow-card);
    transition: transform var(--transition), box-shadow var(--transition);
  }

  .service-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-card-hover);
  }

  .service-card:hover .service-card__accent {
    width: 100%;
  }

  .service-card__accent {
    height: 4px;
    width: 40px;
    background: var(--brand-primary);
    transition: width 0.4s ease;
    border-radius: 0;
  }

  .service-card__content {
    padding: 28px 28px 32px;
    display: flex;
    flex-direction: column;
    flex: 1;
  }

  .service-card__num {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    color: var(--brand-primary);
    letter-spacing: 2px;
    margin-bottom: 12px;
  }

  .service-card__title {
    font-family: var(--font-title);
    font-size: 1.25rem;
    font-weight: var(--fw-bold);
    color: var(--text-primary);
    margin-bottom: 12px;
    line-height: 1.3;
  }

  .service-card__desc {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.7;
    flex: 1;
    margin-bottom: 20px;
  }

  .service-card__link {
    font-family: var(--font-body);
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    color: var(--brand-primary);
    transition: gap var(--transition);
  }

  @media (max-width: 1024px) {
    .services__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .services__grid {
      grid-template-columns: 1fr;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/Process.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import SectionHeading from './design-system/SectionHeading.astro';
const { process } = siteLabels;
---

<section class="process" id="process">
  <div class="container">
    <SectionHeading
      eyebrow={process.sectionLabel}
      title={process.sectionTitle}
      align="center"
    />

    <div class="process__steps">
      {process.items.map((item: { number: string; title: string; description: string }, i: number) => (
        <div class="process-step">
          <div class="process-step__inner">
            <div class="process-step__content">
              <span class="process-step__number-label">{item.number}</span>
              <h3 class="process-step__title">{item.title}</h3>
              <p class="process-step__desc">{item.description}</p>
            </div>
            <div class="process-step__visual" aria-hidden="true">
              <div class="process-step__circle">
                <span class="process-step__circle-num">{item.number}</span>
              </div>
            </div>
          </div>
          {i < process.items.length - 1 && (
            <div class="process-step__connector" aria-hidden="true"></div>
          )}
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .process {
    padding: var(--section-padding);
    background: var(--surface-subtle);
  }

  .process__steps {
    max-width: 860px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .process-step {
    position: relative;
  }

  .process-step__inner {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 48px;
    align-items: center;
    padding: 40px 0;
  }

  /* Even steps: flip layout with RTL trick */
  .process-step:nth-child(even) .process-step__inner {
    direction: rtl;
  }

  .process-step:nth-child(even) .process-step__inner > * {
    direction: ltr;
  }

  .process-step__content {
    display: flex;
    flex-direction: column;
    gap: 10px;
    max-width: 520px;
  }

  .process-step__number-label {
    font-family: var(--font-body);
    font-size: var(--fs-small);
    font-weight: 600;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--brand-primary);
  }

  .process-step__title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    font-weight: 700;
    color: var(--text-primary);
    line-height: 1.2;
    margin: 0;
  }

  .process-step__desc {
    font-size: var(--fs-body);
    line-height: 1.7;
    color: var(--text-secondary);
    margin: 0;
  }

  .process-step__visual {
    flex-shrink: 0;
  }

  .process-step__circle {
    width: 160px;
    height: 160px;
    border-radius: 50%;
    background: var(--brand-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 8px 32px rgba(224, 92, 53, 0.25);
  }

  .process-step__circle-num {
    font-family: var(--font-title);
    font-size: 2.5rem;
    font-weight: 700;
    color: #fff;
    font-style: italic;
  }

  .process-step__connector {
    width: 1px;
    height: 32px;
    background: var(--border-color);
    margin: 0 auto;
    position: relative;
  }

  .process-step__connector::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--brand-primary);
    opacity: 0.5;
  }

  @media (max-width: 768px) {
    .process-step__inner {
      grid-template-columns: 1fr;
      gap: 24px;
      padding: 32px 0;
    }

    .process-step:nth-child(even) .process-step__inner {
      direction: ltr;
    }

    .process-step__visual {
      order: -1;
    }

    .process-step__circle {
      width: 100px;
      height: 100px;
    }

    .process-step__circle-num {
      font-size: 1.75rem;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/Testimonials.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import SectionHeading from './design-system/SectionHeading.astro';
const { testimonials } = siteLabels;
---

<section class="testimonials" id="testimonials">
  <div class="container">
    <SectionHeading
      eyebrow="Client Stories"
      title="WHAT PEOPLE SAY"
      align="center"
    />

    <div class="testimonials__grid">
      {testimonials.map((t: { quote: string; authorName: string; authorRole: string }) => {
        const initial = t.authorName.charAt(0).toUpperCase();
        return (
          <article class="testimonial-card">
            <span class="testimonial-card__quote-mark" aria-hidden="true">"</span>
            <p class="testimonial-card__quote">{t.quote}</p>
            <div class="testimonial-card__author">
              <div class="testimonial-card__avatar" aria-hidden="true">
                {initial}
              </div>
              <div class="testimonial-card__author-info">
                <strong class="testimonial-card__name">{t.authorName}</strong>
                <span class="testimonial-card__role">{t.authorRole}</span>
              </div>
            </div>
          </article>
        );
      })}
    </div>
  </div>
</section>

<style>
  .testimonials {
    padding: var(--section-padding);
    background: var(--surface-base);
  }

  .testimonials__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }

  .testimonial-card {
    background: var(--surface-subtle);
    border-radius: var(--radius-md);
    padding: 36px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    border: 1px solid var(--border-color);
    transition: transform var(--transition), box-shadow var(--transition);
  }

  .testimonial-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-card-hover);
  }

  .testimonial-card__quote-mark {
    font-family: var(--font-title);
    font-style: italic;
    font-size: 3.5rem;
    color: var(--brand-primary);
    line-height: 0.5;
    display: block;
    margin-bottom: 8px;
  }

  .testimonial-card__quote {
    font-size: var(--fs-body);
    line-height: 1.7;
    color: var(--text-secondary);
    flex: 1;
    margin: 0;
  }

  .testimonial-card__author {
    display: flex;
    align-items: center;
    gap: 14px;
    padding-top: 16px;
    border-top: 1px solid var(--border-color);
  }

  .testimonial-card__avatar {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: var(--brand-primary);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-title);
    font-size: 1.1rem;
    font-weight: 700;
    flex-shrink: 0;
  }

  .testimonial-card__author-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .testimonial-card__name {
    font-size: var(--fs-small);
    font-weight: 600;
    color: var(--text-primary);
    display: block;
  }

  .testimonial-card__role {
    font-size: var(--fs-xs);
    color: var(--text-muted);
    display: block;
    line-height: 1.4;
  }

  @media (max-width: 768px) {
    .testimonials__grid {
      grid-template-columns: 1fr;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/Stats.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
const { stats } = siteLabels;
---

<section class="stats">
  <div class="container">
    <div class="stats__grid">
      {stats.items.map((item: { number: string; label: string }) => (
        <div class="stats__item">
          <span class="stats__number">{item.number}</span>
          <span class="stats__label">{item.label}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .stats {
    padding: 72px 0;
    background: var(--surface-subtle);
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
  }

  .stats__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
  }

  .stats__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    padding: 24px 20px;
    text-align: center;
    position: relative;
  }

  .stats__item + .stats__item::before {
    content: '';
    position: absolute;
    left: 0;
    top: 20%;
    height: 60%;
    width: 1px;
    background: var(--border-color);
  }

  .stats__number {
    font-family: var(--font-title);
    font-size: 3rem;
    font-weight: 700;
    color: var(--brand-primary);
    line-height: 1;
    letter-spacing: -0.02em;
  }

  .stats__label {
    font-size: var(--fs-small);
    font-weight: 500;
    color: var(--text-secondary);
    letter-spacing: 0.3px;
    text-transform: uppercase;
    letter-spacing: 1.5px;
  }

  @media (max-width: 768px) {
    .stats__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 24px 0;
    }

    .stats__item:nth-child(odd)::before {
      display: none;
    }

    .stats__number {
      font-size: 2.25rem;
    }
  }

  @media (max-width: 420px) {
    .stats__grid {
      grid-template-columns: 1fr;
    }

    .stats__item::before {
      display: none !important;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/CTA.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
import ActionButton from './design-system/ActionButton.astro';
const { cta } = siteLabels;
const base = import.meta.env.BASE_URL;
---

<section class="cta-section">
  <div class="container">
    <div class="cta-section__inner">
      <h2 class="cta-section__title">{cta.title}</h2>
      <p class="cta-section__desc">{cta.description}</p>
      <div class="cta-section__actions">
        <ActionButton href={`${base}contact`} label={cta.primary} variant="outline-light" />
        <ActionButton href={`${base}services`} label={cta.secondary} variant="outline-light" />
      </div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    background: var(--brand-primary);
    padding: 96px 0;
    position: relative;
    overflow: hidden;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    top: -40%;
    right: -10%;
    width: 500px;
    height: 500px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.06);
    pointer-events: none;
  }

  .cta-section::after {
    content: '';
    position: absolute;
    bottom: -30%;
    left: -5%;
    width: 350px;
    height: 350px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.04);
    pointer-events: none;
  }

  .cta-section__inner {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
  }

  .cta-section__title {
    font-family: var(--font-title);
    font-style: italic;
    font-size: var(--fs-h1);
    font-weight: 600;
    color: #fff;
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin: 0;
  }

  .cta-section__desc {
    font-size: var(--fs-body);
    color: rgba(255, 255, 255, 0.82);
    line-height: 1.7;
    max-width: 520px;
    margin: 0;
  }

  .cta-section__actions {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 8px;
  }

  @media (max-width: 768px) {
    .cta-section {
      padding: 72px 0;
    }

    .cta-section__title {
      font-size: var(--fs-h2);
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/Footer.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';
const { footer, header } = siteLabels;
const base = import.meta.env.BASE_URL;
const year = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer__inner container">
    <div class="footer__brand">
      <a href={base} class="footer__logo">{footer.logo}</a>
      <p class="footer__tagline">{footer.tagline}</p>
    </div>

    <nav class="footer__nav" aria-label="Footer navigation">
      <ul class="footer__nav-list">
        {header.navLinks.map((link: { label: string; href: string }) => {
          const fullHref = link.href === '/' ? base : `${base}${link.href.replace(/^\//, '')}`;
          return (
            <li>
              <a href={fullHref} class="footer__nav-link">{link.label}</a>
            </li>
          );
        })}
      </ul>
    </nav>

    <div class="footer__contact">
      <p class="footer__contact-label">Ready to start?</p>
      <a href={`${base}contact`} class="footer__contact-link">Book a free call →</a>
    </div>
  </div>

  <div class="footer__bottom">
    <div class="container">
      <p class="footer__copyright">© {year} {footer.copyrightSuffix}</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--bg-dark);
    color: var(--text-on-dark);
  }

  .footer__inner {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 48px;
    padding-top: 72px;
    padding-bottom: 56px;
    border-bottom: 1px solid rgba(253, 248, 240, 0.1);
  }

  .footer__logo {
    display: inline-block;
    font-family: var(--font-title);
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--brand-primary);
    margin-bottom: 16px;
    letter-spacing: -0.01em;
  }

  .footer__tagline {
    font-size: var(--fs-small);
    color: rgba(253, 248, 240, 0.55);
    line-height: 1.7;
    max-width: 300px;
    margin: 0;
  }

  .footer__nav-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .footer__nav-link {
    font-size: var(--fs-small);
    color: rgba(253, 248, 240, 0.65);
    transition: color var(--transition);
    font-weight: 500;
  }

  .footer__nav-link:hover {
    color: var(--text-on-dark);
  }

  .footer__contact-label {
    font-size: var(--fs-small);
    color: rgba(253, 248, 240, 0.55);
    margin-bottom: 10px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .footer__contact-link {
    font-family: var(--font-title);
    font-style: italic;
    font-size: 1.1rem;
    color: var(--brand-primary);
    transition: color var(--transition);
    display: inline-block;
  }

  .footer__contact-link:hover {
    color: #f07050;
  }

  .footer__bottom {
    padding: 24px 0;
  }

  .footer__copyright {
    font-size: var(--fs-xs);
    color: rgba(253, 248, 240, 0.35);
    margin: 0;
  }

  @media (max-width: 768px) {
    .footer__inner {
      grid-template-columns: 1fr;
      gap: 40px;
      padding-top: 56px;
      padding-bottom: 40px;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/design-system/ActionButton.astro
======================================================================
---
interface Props {
  href: string;
  label: string;
  variant?: 'primary' | 'outline' | 'outline-light';
  className?: string;
}

const { href, label, variant = 'primary', className = '' } = Astro.props;
---

<a
  href={href}
  class={`ds-action-button ds-action-button--${variant} ${className}`.trim()}
>
  <span class="ds-action-button__label">{label}</span>
</a>

<style>
  .ds-action-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 14px 32px;
    font-family: var(--font-button);
    font-size: var(--fs-small);
    font-weight: 600;
    letter-spacing: 0.5px;
    border-radius: var(--radius-sm);
    border: 2px solid transparent;
    transition: background-color var(--transition), color var(--transition), border-color var(--transition), transform var(--transition), box-shadow var(--transition);
    cursor: pointer;
    white-space: nowrap;
  }

  .ds-action-button:hover {
    transform: translateY(-1px);
  }

  .ds-action-button--primary {
    background: var(--brand-primary);
    color: #fff;
    border-color: var(--brand-primary);
  }

  .ds-action-button--primary:hover {
    background: var(--brand-primary-dark);
    border-color: var(--brand-primary-dark);
    box-shadow: 0 6px 24px rgba(224, 92, 53, 0.3);
  }

  .ds-action-button--outline {
    background: transparent;
    color: var(--text-primary);
    border-color: rgba(26, 14, 8, 0.3);
  }

  .ds-action-button--outline:hover {
    background: var(--surface-subtle);
    border-color: var(--text-primary);
  }

  .ds-action-button--outline-light {
    background: transparent;
    color: #fff;
    border-color: rgba(255, 255, 255, 0.6);
  }

  .ds-action-button--outline-light:hover {
    background: rgba(255, 255, 255, 0.15);
    border-color: #fff;
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/design-system/SectionBlock.astro
======================================================================
---
interface Props {
  id?: string;
  className?: string;
  tone?: 'base' | 'subtle' | 'dark' | 'coral' | 'none';
  padding?: 'default' | 'compact' | 'tight' | 'none';
}

const {
  id,
  className = '',
  tone = 'base',
  padding = 'default',
} = Astro.props;
---

<section
  id={id}
  class={`ds-section ds-section--${tone} ds-section--pad-${padding} ${className}`.trim()}
>
  <slot />
</section>

<style>
  .ds-section {
    width: 100%;
  }

  .ds-section--pad-default {
    padding: var(--section-padding);
  }

  .ds-section--pad-compact {
    padding: 64px 0;
  }

  .ds-section--pad-tight {
    padding: 40px 0;
  }

  .ds-section--pad-none {
    padding: 0;
  }

  .ds-section--base {
    background: var(--surface-base);
  }

  .ds-section--subtle {
    background: var(--surface-subtle);
  }

  .ds-section--dark {
    background: var(--bg-dark);
  }

  .ds-section--coral {
    background: var(--brand-primary);
  }

  .ds-section--none {
    background: transparent;
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/design-system/SectionHeading.astro
======================================================================
---
interface Props {
  eyebrow?: string;
  title: string;
  subtitle?: string;
  align?: 'left' | 'center';
  className?: string;
  onDark?: boolean;
}

const {
  eyebrow,
  title,
  subtitle,
  align = 'left',
  className = '',
  onDark = false,
} = Astro.props;
---

<div class={`section-heading section-heading--${align} ${onDark ? 'section-heading--on-dark' : ''} ${className}`.trim()}>
  {eyebrow && <p class="section-heading__eyebrow">{eyebrow}</p>}
  <h2 class="section-heading__title">{title}</h2>
  {subtitle && <p class="section-heading__subtitle">{subtitle}</p>}
</div>

<style>
  .section-heading {
    margin-bottom: 56px;
  }

  .section-heading--center {
    text-align: center;
  }

  .section-heading--center .section-heading__subtitle {
    margin-left: auto;
    margin-right: auto;
  }

  .section-heading__eyebrow {
    font-family: var(--font-body);
    font-size: var(--fs-small);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 2.5px;
    color: var(--brand-primary);
    margin-bottom: 12px;
    line-height: 1.4;
  }

  .section-heading--on-dark .section-heading__eyebrow {
    color: rgba(253, 248, 240, 0.7);
  }

  .section-heading__title {
    font-family: var(--font-title);
    font-size: var(--fs-h2);
    font-weight: 700;
    letter-spacing: -0.02em;
    line-height: 1.1;
    color: var(--text-primary);
    margin-bottom: 16px;
  }

  .section-heading--on-dark .section-heading__title {
    color: var(--text-on-dark);
  }

  .section-heading__subtitle {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 560px;
  }

  .section-heading--on-dark .section-heading__subtitle {
    color: rgba(253, 248, 240, 0.75);
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/widgets/BookingWidget.astro
======================================================================
---
interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Book a Free 20-Minute Call',
  description = 'No pitch. No pressure. Just an honest conversation about where you are and whether coaching can help.',
} = Astro.props;
---

<div class="booking-widget">
  <div class="booking-widget__header">
    <h3 class="booking-widget__title">{title}</h3>
    <p class="booking-widget__desc">{description}</p>
  </div>

  <form class="booking-widget__form" data-booking-form>
    <div class="form-row">
      <div class="form-group">
        <label for="booking-name" class="form-label">Your name</label>
        <input
          type="text"
          id="booking-name"
          name="name"
          class="form-input"
          placeholder="Alex Johnson"
          required
        />
      </div>
      <div class="form-group">
        <label for="booking-email" class="form-label">Email address</label>
        <input
          type="email"
          id="booking-email"
          name="email"
          class="form-input"
          placeholder="alex@example.com"
          required
        />
      </div>
    </div>

    <div class="form-group">
      <label for="booking-goal" class="form-label">What's the one thing you most want to change?</label>
      <textarea
        id="booking-goal"
        name="goal"
        class="form-input form-textarea"
        rows="3"
        placeholder="Tell me a little about where you are and what you're working towards..."
      ></textarea>
    </div>

    <button type="submit" class="booking-widget__submit">
      Request My Free Call
    </button>

    <p class="booking-widget__note">I'll respond within 24 hours to confirm a time that works.</p>
  </form>

  <div class="booking-widget__success" aria-hidden="true">
    <div class="booking-widget__success-icon" aria-hidden="true">✓</div>
    <h4 class="booking-widget__success-title">Request received!</h4>
    <p class="booking-widget__success-desc">I'll be in touch within 24 hours to confirm your free call. Looking forward to speaking with you.</p>
  </div>
</div>

<script>
  const form = document.querySelector('[data-booking-form]') as HTMLFormElement;
  const successEl = document.querySelector('.booking-widget__success') as HTMLElement;

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const data = new FormData(form);
    const submit = form.querySelector('button[type="submit"]') as HTMLButtonElement;
    submit.disabled = true;
    submit.textContent = 'Sending...';

    try {
      await fetch('https://flowstarter.dev/api/leads/capture', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          type: 'booking',
          name: data.get('name'),
          email: data.get('email'),
          goal: data.get('goal'),
        }),
      });
    } catch (_) {
      // Silent fail — show success regardless for demo
    }

    form.style.display = 'none';
    if (successEl) {
      successEl.removeAttribute('aria-hidden');
      successEl.style.display = 'flex';
    }
  });
</script>

<style>
  .booking-widget {
    background: var(--surface-field);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    padding: 40px;
  }

  .booking-widget__header {
    margin-bottom: 28px;
  }

  .booking-widget__title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 10px;
  }

  .booking-widget__desc {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.65;
    margin: 0;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-bottom: 16px;
  }

  .form-label {
    font-size: var(--fs-small);
    font-weight: 500;
    color: var(--text-primary);
  }

  .form-input {
    width: 100%;
    padding: 11px 14px;
    font-family: var(--font-body);
    font-size: var(--fs-small);
    color: var(--text-primary);
    background: var(--surface-base);
    border: 1.5px solid var(--surface-field-border);
    border-radius: var(--radius-sm);
    transition: border-color var(--transition), box-shadow var(--transition);
    outline: none;
    appearance: none;
  }

  .form-input:focus {
    border-color: var(--brand-primary);
    box-shadow: 0 0 0 3px rgba(224, 92, 53, 0.12);
  }

  .form-input::placeholder {
    color: var(--text-muted);
  }

  .form-textarea {
    resize: vertical;
    min-height: 80px;
  }

  .booking-widget__submit {
    width: 100%;
    padding: 14px 24px;
    background: var(--brand-primary);
    color: #fff;
    font-family: var(--font-button);
    font-size: var(--fs-small);
    font-weight: 600;
    border-radius: var(--radius-sm);
    border: none;
    cursor: pointer;
    transition: background-color var(--transition), transform var(--transition);
    margin-top: 4px;
  }

  .booking-widget__submit:hover {
    background: var(--brand-primary-dark);
    transform: translateY(-1px);
  }

  .booking-widget__submit:disabled {
    opacity: 0.65;
    cursor: not-allowed;
    transform: none;
  }

  .booking-widget__note {
    font-size: var(--fs-xs);
    color: var(--text-muted);
    text-align: center;
    margin: 12px 0 0;
  }

  /* Success state */
  .booking-widget__success {
    display: none;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 12px;
    padding: 24px 0;
  }

  .booking-widget__success-icon {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: var(--brand-primary);
    color: #fff;
    font-size: 1.5rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .booking-widget__success-title {
    font-family: var(--font-title);
    font-size: var(--fs-h4);
    font-weight: 700;
    color: var(--text-primary);
    margin: 0;
  }

  .booking-widget__success-desc {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.65;
    max-width: 400px;
    margin: 0;
  }

  @media (max-width: 640px) {
    .booking-widget {
      padding: 28px 20px;
    }

    .form-row {
      grid-template-columns: 1fr;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/widgets/NewsletterWidget.astro
======================================================================
---
interface Props {
  title?: string;
  description?: string;
  placeholder?: string;
  buttonLabel?: string;
}

const {
  title = 'Weekly coaching insights',
  description = 'Short, practical notes on mindset, clarity, and momentum. No filler.',
  placeholder = 'Your email address',
  buttonLabel = 'Subscribe',
} = Astro.props;
---

<div class="newsletter-widget">
  <div class="newsletter-widget__text">
    <h4 class="newsletter-widget__title">{title}</h4>
    <p class="newsletter-widget__desc">{description}</p>
  </div>

  <form class="newsletter-widget__form" data-newsletter-form>
    <div class="newsletter-widget__input-row">
      <input
        type="email"
        name="email"
        class="newsletter-widget__input"
        placeholder={placeholder}
        required
        aria-label="Email address"
      />
      <button type="submit" class="newsletter-widget__btn">{buttonLabel}</button>
    </div>
    <p class="newsletter-widget__note">No spam. Unsubscribe any time.</p>
  </form>

  <p class="newsletter-widget__confirm" aria-hidden="true">
    You're in. Watch your inbox.
  </p>
</div>

<script>
  const form = document.querySelector('[data-newsletter-form]') as HTMLFormElement;
  const confirm = document.querySelector('.newsletter-widget__confirm') as HTMLElement;

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const data = new FormData(form);
    const btn = form.querySelector('button') as HTMLButtonElement;
    btn.disabled = true;
    btn.textContent = '...';

    try {
      await fetch('https://flowstarter.dev/api/leads/capture', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ type: 'newsletter', email: data.get('email') }),
      });
    } catch (_) {}

    form.style.display = 'none';
    if (confirm) {
      confirm.removeAttribute('aria-hidden');
    }
  });
</script>

<style>
  .newsletter-widget {
    background: var(--surface-subtle);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    padding: 36px;
  }

  .newsletter-widget__text {
    margin-bottom: 20px;
  }

  .newsletter-widget__title {
    font-family: var(--font-title);
    font-size: var(--fs-h4);
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 8px;
  }

  .newsletter-widget__desc {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  .newsletter-widget__input-row {
    display: flex;
    gap: 10px;
  }

  .newsletter-widget__input {
    flex: 1;
    padding: 11px 14px;
    font-family: var(--font-body);
    font-size: var(--fs-small);
    color: var(--text-primary);
    background: var(--surface-base);
    border: 1.5px solid var(--surface-field-border);
    border-radius: var(--radius-sm);
    transition: border-color var(--transition), box-shadow var(--transition);
    outline: none;
    appearance: none;
    min-width: 0;
  }

  .newsletter-widget__input:focus {
    border-color: var(--brand-primary);
    box-shadow: 0 0 0 3px rgba(224, 92, 53, 0.12);
  }

  .newsletter-widget__input::placeholder {
    color: var(--text-muted);
  }

  .newsletter-widget__btn {
    padding: 11px 22px;
    background: var(--brand-primary);
    color: #fff;
    font-family: var(--font-button);
    font-size: var(--fs-small);
    font-weight: 600;
    border-radius: var(--radius-sm);
    border: none;
    cursor: pointer;
    flex-shrink: 0;
    transition: background-color var(--transition);
  }

  .newsletter-widget__btn:hover {
    background: var(--brand-primary-dark);
  }

  .newsletter-widget__btn:disabled {
    opacity: 0.65;
    cursor: not-allowed;
  }

  .newsletter-widget__note {
    font-size: var(--fs-xs);
    color: var(--text-muted);
    margin: 10px 0 0;
  }

  .newsletter-widget__confirm {
    font-size: var(--fs-small);
    font-weight: 500;
    color: var(--brand-primary);
  }

  .newsletter-widget__confirm[aria-hidden="true"] {
    display: none;
  }

  @media (max-width: 480px) {
    .newsletter-widget__input-row {
      flex-direction: column;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/components/widgets/LeadCaptureWidget.astro
======================================================================
---
interface Props {
  title?: string;
  description?: string;
  className?: string;
}
const {
  title = "Let's Talk",
  description = "Tell me a little about where you are and what you're looking for. I'll come back to you within 24 hours.",
  className = '',
} = Astro.props;
---

<div class={`lead-capture-widget ${className}`} data-project-id="{{PROJECT_ID}}">
  <div class="lead-capture-widget__header">
    <h2 class="lead-capture-widget__title">{title}</h2>
    <p class="lead-capture-widget__desc">{description}</p>
  </div>

  <form class="lead-capture-widget__form" data-lead-form novalidate>
    <div class="lead-capture-widget__row">
      <div class="lead-capture-widget__field">
        <label class="lead-capture-widget__label" for="lcw-name">Your name</label>
        <input id="lcw-name" name="name" type="text" required
               placeholder="First and last name"
               class="lead-capture-widget__input" autocomplete="name" />
      </div>
      <div class="lead-capture-widget__field">
        <label class="lead-capture-widget__label" for="lcw-email">Email address</label>
        <input id="lcw-email" name="email" type="email" required
               placeholder="you@example.com"
               class="lead-capture-widget__input" autocomplete="email" />
      </div>
    </div>

    <div class="lead-capture-widget__field">
      <label class="lead-capture-widget__label" for="lcw-phone">Phone <span class="lead-capture-widget__optional">(optional)</span></label>
      <input id="lcw-phone" name="phone" type="tel"
             placeholder="+1 (555) 000-0000"
             class="lead-capture-widget__input" autocomplete="tel" />
    </div>

    <div class="lead-capture-widget__field">
      <label class="lead-capture-widget__label" for="lcw-message">What's on your mind?</label>
      <textarea id="lcw-message" name="message" required
                placeholder="Tell me where you are right now, what's challenging you, and what you want to change. The more honest you are, the more I can help."
                class="lead-capture-widget__textarea"
                rows="5"></textarea>
    </div>

    <div class="lead-capture-widget__footer">
      <button type="submit" class="lead-capture-widget__submit" data-lead-submit>
        Send Message
      </button>
      <p class="lead-capture-widget__success" hidden aria-live="polite">
        ✓ Got it! I'll be back to you within 24 hours.
      </p>
      <p class="lead-capture-widget__error" hidden aria-live="polite">
        Something went wrong. Please try again.
      </p>
    </div>
  </form>
</div>

<script>
  document.querySelectorAll('[data-lead-form]').forEach(form => {
    form.addEventListener('submit', async e => {
      e.preventDefault();
      const widget = form.closest('[data-project-id]');
      const projectId = widget?.dataset.projectId ?? '';
      const btn = form.querySelector('[data-lead-submit]');
      const success = form.querySelector('.lead-capture-widget__success');
      const error = form.querySelector('.lead-capture-widget__error');
      const data = {
        projectId,
        name: form.querySelector('[name="name"]')?.value ?? '',
        email: form.querySelector('[name="email"]')?.value ?? '',
        phone: form.querySelector('[name="phone"]')?.value ?? '',
        message: form.querySelector('[name="message"]')?.value ?? '',
        source: 'contact',
      };
      const prev = btn.textContent;
      btn.disabled = true;
      btn.textContent = 'Sending…';
      success.hidden = true;
      error.hidden = true;
      try {
        const res = await fetch('https://flowstarter.dev/api/leads/capture', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
        });
        if (res.ok) {
          success.hidden = false;
          form.querySelectorAll('input, textarea').forEach(el => el.value = '');
        } else {
          error.hidden = false;
        }
      } catch {
        error.hidden = false;
      } finally {
        btn.disabled = false;
        btn.textContent = prev;
      }
    });
  });
</script>

<style>
  .lead-capture-widget {
    background: var(--surface-subtle);
    border: 1px solid var(--border-color);
    padding: 48px;
    border-radius: var(--radius-md);
  }

  .lead-capture-widget__header {
    margin-bottom: 32px;
  }

  .lead-capture-widget__title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    color: var(--text-primary);
    margin-bottom: 10px;
    font-style: italic;
  }

  .lead-capture-widget__desc {
    color: var(--text-secondary);
    line-height: 1.65;
  }

  .lead-capture-widget__row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
  }

  .lead-capture-widget__field {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 20px;
  }

  .lead-capture-widget__label {
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    color: var(--text-primary);
  }

  .lead-capture-widget__optional {
    color: var(--text-muted);
    font-weight: var(--fw-regular);
  }

  .lead-capture-widget__input,
  .lead-capture-widget__textarea {
    background: var(--surface-field);
    border: 1px solid var(--surface-field-border);
    color: var(--text-primary);
    font-family: var(--font-body);
    font-size: var(--fs-body);
    padding: 12px 16px;
    width: 100%;
    border-radius: var(--radius-sm);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .lead-capture-widget__input::placeholder,
  .lead-capture-widget__textarea::placeholder {
    color: var(--text-muted);
  }

  .lead-capture-widget__input:focus,
  .lead-capture-widget__textarea:focus {
    outline: none;
    border-color: var(--brand-primary);
    box-shadow: 0 0 0 3px color-mix(in srgb, var(--brand-primary) 12%, transparent);
  }

  .lead-capture-widget__textarea {
    resize: vertical;
    min-height: 140px;
  }

  .lead-capture-widget__footer {
    margin-top: 8px;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .lead-capture-widget__submit {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 14px 36px;
    background: var(--brand-primary);
    color: #fff;
    font-family: var(--font-button);
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    border: none;
    border-radius: var(--radius-sm);
    cursor: pointer;
    align-self: flex-start;
    transition: opacity 0.25s ease;
  }

  .lead-capture-widget__submit:hover { opacity: 0.88; }
  .lead-capture-widget__submit:disabled { opacity: 0.5; cursor: not-allowed; }

  .lead-capture-widget__success {
    color: var(--brand-primary);
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
  }

  .lead-capture-widget__error {
    color: #c0392b;
    font-size: var(--fs-small);
  }

  @media (max-width: 768px) {
    .lead-capture-widget { padding: 32px 24px; }
    .lead-capture-widget__row { grid-template-columns: 1fr; }
  }
</style>


======================================================================
// REFERENCE: coach-pro/layouts/Layout.astro
======================================================================
---
import { frontmatter as siteLabels } from '../content/site-labels.md';

interface Props {
  title?: string;
}

const { siteMeta } = siteLabels;
const { title = siteMeta.title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={siteMeta.description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;0,9..144,700;1,9..144,400;1,9..144,600&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet" />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>


======================================================================
// REFERENCE: coach-pro/styles/global.css
======================================================================
/* Reset */
*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
  scrollbar-gutter: stable;
}

body {
  font-family: var(--font-body);
  background-color: var(--surface-base);
  color: var(--text-primary);
  font-size: var(--fs-body);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

img {
  max-width: 100%;
  display: block;
}

p {
  font-size: var(--fs-body);
  color: var(--text-secondary);
  line-height: 1.7;
}

a {
  color: inherit;
  text-decoration: none;
}

ul,
ol {
  list-style: none;
}

button {
  font: inherit;
  cursor: pointer;
  border: none;
  background: none;
  border-radius: 0;
  font-family: var(--font-button);
}

/* Design Tokens */
:root {
  /* Colors */
  --bg-dark: #1A0E08;
  --brand-primary: #E05C35;
  --brand-primary-dark: #c44d28;
  --surface-base: #FDF8F0;
  --surface-subtle: #F5EDE0;
  --surface-field: #FFF8F2;
  --surface-field-border: rgba(26, 14, 8, 0.15);
  --text-primary: #1A0E08;
  --text-on-dark: #FDF8F0;
  --text-secondary: #6B5040;
  --text-muted: #9B7860;
  --border-color: rgba(26, 14, 8, 0.1);
  --font-title: 'Fraunces', Georgia, serif;
  --font-body: 'Inter', sans-serif;
  --font-button: 'Inter', sans-serif;

  /* Spacing */
  --section-padding: 100px 0;
  --container-width: 1200px;
  --container-padding: 0 24px;

  /* Typography */
  --fs-display: 3.5rem;
  --fs-h1: 3rem;
  --fs-h2: 2.5rem;
  --fs-h3: 1.75rem;
  --fs-h4: 1.375rem;
  --fs-h5: 1.15rem;
  --fs-body: 1.05rem;
  --fs-small: 0.875rem;
  --fs-xs: 0.75rem;

  --fw-light: 300;
  --fw-regular: 400;
  --fw-medium: 500;
  --fw-semibold: 600;
  --fw-bold: 700;

  /* Borders */
  --radius-sm: 6px;
  --radius-md: 12px;
  --radius-lg: 20px;

  /* Transitions */
  --transition: 0.25s ease;

  /* Shadows */
  --shadow-card: 0 4px 24px rgba(26, 14, 8, 0.06);
  --shadow-card-hover: 0 12px 40px rgba(26, 14, 8, 0.12);
}

/* Utilities */
.container {
  max-width: var(--container-width);
  margin: 0 auto;
  padding: var(--container-padding);
}

h1,
h2,
h3,
h4,
h5,
h6 {
  font-family: var(--font-title);
  color: var(--text-primary);
  line-height: 1.15;
}

h1 { font-size: var(--fs-h1); font-weight: 700; }
h2 { font-size: var(--fs-h2); font-weight: 700; }
h3 { font-size: var(--fs-h3); font-weight: 600; }
h4 { font-size: var(--fs-h4); font-weight: 600; }
h5 { font-size: var(--fs-h5); font-weight: 500; }

.section-eyebrow {
  font-family: var(--font-body);
  font-size: var(--fs-small);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 2.5px;
  color: var(--brand-primary);
  margin-bottom: 16px;
}

.section-title {
  font-family: var(--font-title);
  font-size: var(--fs-h2);
  font-weight: 700;
  letter-spacing: -0.02em;
  line-height: 1.1;
  color: var(--text-primary);
}

/* Visibility animation support */
.is-observing {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity 0.55s ease, transform 0.55s ease;
}

.is-observing.is-visible {
  opacity: 1;
  transform: translateY(0);
}

/* Responsive */
@media (max-width: 1024px) {
  :root {
    --fs-display: 2.8rem;
    --fs-h1: 2.5rem;
    --fs-h2: 2rem;
    --fs-h3: 1.5rem;
  }
}

@media (max-width: 768px) {
  :root {
    --section-padding: 72px 0;
    --container-padding: 0 20px;
    --fs-display: 2.2rem;
    --fs-h1: 2rem;
    --fs-h2: 1.65rem;
    --fs-h3: 1.3rem;
    --fs-h4: 1.15rem;
    --fs-body: 1rem;
  }
}


======================================================================
// REFERENCE: coach-pro/pages/index.astro
======================================================================
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import Services from '../components/Services.astro';
import Process from '../components/Process.astro';
import Testimonials from '../components/Testimonials.astro';
import CTA from '../components/CTA.astro';
import Footer from '../components/Footer.astro';
import SectionBlock from '../components/design-system/SectionBlock.astro';
import { frontmatter as siteLabels } from '../content/site-labels.md';

const { stats } = siteLabels;
---

<Layout>
  <Header activeIndex={0} />
  <main>
    <Hero />

    <!-- Stats bar -->
    <div class="stats-bar">
      <div class="container">
        <div class="stats-bar__inner">
          {stats.items.map((s) => (
            <div class="stat-item">
              <span class="stat-item__number">{s.number}</span>
              <span class="stat-item__label">{s.label}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <Services />
    <Process />
    <Testimonials />
    <CTA />
  </main>
  <Footer />
</Layout>

<style>
  .stats-bar {
    background: var(--surface-card);
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
  }

  .stats-bar__inner {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }

  .stat-item {
    padding: 28px 32px;
    border-right: 1px solid var(--border-color);
    display: flex;
    flex-direction: column;
    gap: 6px;
    align-items: center;
    text-align: center;
  }

  .stat-item:last-child {
    border-right: none;
  }

  .stat-item__number {
    font-family: var(--font-title);
    font-size: clamp(1.6rem, 2.5vw, 2.2rem);
    font-weight: var(--fw-headline);
    color: var(--brand-primary);
    line-height: 1;
  }

  .stat-item__label {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-medium);
    color: var(--text-secondary);
  }

  @media (max-width: 768px) {
    .stats-bar__inner {
      grid-template-columns: repeat(2, 1fr);
    }

    .stat-item {
      padding: 20px 16px;
      border-bottom: 1px solid var(--border-color);
    }

    .stat-item:nth-child(even) {
      border-right: none;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/pages/about.astro
======================================================================
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SectionBlock from '../components/design-system/SectionBlock.astro';
import ActionButton from '../components/design-system/ActionButton.astro';
import { frontmatter as siteLabels } from '../content/site-labels.md';

const { siteMeta } = siteLabels;
const base = import.meta.env.BASE_URL;
---

<Layout title={`About — ${siteMeta.title}`}>
  <Header activeIndex={1} />
  <main>
    <!-- Page Hero -->
    <div class="page-hero">
      <div class="container">
        <div class="page-hero__inner">
          <div class="page-hero__content">
            <p class="page-hero__eyebrow">About Me</p>
            <h1 class="page-hero__title">I believe everyone deserves to live a life they're proud of.</h1>
            <p class="page-hero__sub">I'm a certified coach with 6 years of experience helping ambitious people break through plateaus, find clarity, and take bold action toward the life they actually want.</p>
            <div class="page-hero__actions">
              <ActionButton href={`${base}contact`} label="Book a Free Call" variant="primary" />
            </div>
          </div>
          <div class="page-hero__visual" aria-hidden="true">
            <div class="page-hero__shape"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Story section -->
    <SectionBlock tone="light" padding="default" className="about-story">
      <div class="container">
        <div class="about-story__grid">
          <div class="about-story__content">
            <p class="section-label">My Story</p>
            <h2 class="about-story__title">I've been where you are.</h2>
            <p>A few years ago, I was stuck. Good job, on paper doing everything right — but something felt off. I wasn't living the life I actually wanted. I was living the life I thought I was supposed to want.</p>
            <p>Coaching changed that for me. And now I help others make the same shift. Not through generic advice or cookie-cutter frameworks — through real conversations, real accountability, and real change.</p>
            <p>I hold certifications from the International Coaching Federation (ICF) and have trained in evidence-based approaches including motivational interviewing, positive psychology, and cognitive behavioural coaching.</p>
          </div>
          <div class="about-story__values">
            {[
              { icon: "✦", title: "Human-first", desc: "Every person is unique. I don't use scripts or templates — I meet you where you are." },
              { icon: "◆", title: "Results-focused", desc: "Coaching should change things. We measure progress and hold each other accountable." },
              { icon: "●", title: "Honest", desc: "I'll tell you what you need to hear, not just what you want to hear. That's what real support looks like." },
            ].map((v) => (
              <div class="about-value">
                <span class="about-value__icon">{v.icon}</span>
                <div>
                  <h3 class="about-value__title">{v.title}</h3>
                  <p class="about-value__desc">{v.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </SectionBlock>

    <!-- Credentials -->
    <SectionBlock tone="warm" padding="default" className="credentials">
      <div class="container">
        <p class="section-label">Credentials</p>
        <h2 class="section-title" style="margin-bottom: 40px;">TRAINED & CERTIFIED</h2>
        <div class="credentials__grid">
          {[
            { title: "ICF Certified Coach", sub: "International Coaching Federation, 2020" },
            { title: "Positive Psychology Practitioner", sub: "CAPP Certification, 2021" },
            { title: "CBT Coaching", sub: "Cognitive Behavioural Coaching Diploma, 2019" },
            { title: "Executive Coaching", sub: "Advanced Programme, 2022" },
          ].map((c) => (
            <div class="credential-card">
              <span class="credential-card__check">✓</span>
              <div>
                <h3 class="credential-card__title">{c.title}</h3>
                <p class="credential-card__sub">{c.sub}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </SectionBlock>
  </main>
  <Footer />
</Layout>

<style>
  .page-hero {
    background: var(--surface-base);
    padding: 120px 0 80px;
    border-bottom: 1px solid var(--border-color);
  }

  .page-hero__inner {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 60px;
    align-items: center;
  }

  .page-hero__eyebrow {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--brand-primary);
    margin-bottom: 20px;
  }

  .page-hero__title {
    font-family: var(--font-title);
    font-size: clamp(2rem, 3.5vw, 3rem);
    font-weight: var(--fw-headline);
    color: var(--text-primary);
    line-height: 1.2;
    margin-bottom: 20px;
  }

  .page-hero__sub {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 560px;
    margin-bottom: 32px;
  }

  .page-hero__shape {
    width: 240px;
    height: 240px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--brand-primary), #f08050);
    opacity: 0.15;
    margin: 0 auto;
  }

  :global(.about-story) {
    background: var(--surface-base);
  }

  .about-story__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: start;
  }

  .about-story__content {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .about-story__title {
    font-family: var(--font-title);
    font-size: var(--fs-h2);
    font-weight: var(--fw-headline);
    color: var(--text-primary);
    line-height: 1.2;
    margin-bottom: 8px;
  }

  .about-story__content p {
    color: var(--text-secondary);
    line-height: 1.7;
  }

  .about-story__values {
    display: flex;
    flex-direction: column;
    gap: 24px;
    padding-top: 8px;
  }

  .about-value {
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }

  .about-value__icon {
    font-size: 1.2rem;
    color: var(--brand-primary);
    flex-shrink: 0;
    margin-top: 2px;
  }

  .about-value__title {
    font-family: var(--font-title);
    font-size: var(--fs-h4);
    font-weight: var(--fw-bold);
    color: var(--text-primary);
    margin-bottom: 6px;
  }

  .about-value__desc {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    line-height: 1.65;
  }

  :global(.credentials) {
    background: var(--surface-warm);
  }

  .credentials__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }

  .credential-card {
    background: var(--surface-card);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    padding: 24px;
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }

  .credential-card__check {
    width: 28px;
    height: 28px;
    background: var(--brand-primary);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: var(--fw-bold);
    flex-shrink: 0;
  }

  .credential-card__title {
    font-family: var(--font-title);
    font-size: var(--fs-h4);
    font-weight: var(--fw-bold);
    color: var(--text-primary);
    margin-bottom: 4px;
  }

  .credential-card__sub {
    font-size: var(--fs-xs);
    color: var(--text-secondary);
  }

  @media (max-width: 900px) {
    .page-hero__inner {
      grid-template-columns: 1fr;
    }

    .page-hero__shape {
      display: none;
    }

    .about-story__grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }

    .credentials__grid {
      grid-template-columns: 1fr;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/pages/services.astro
======================================================================
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Services from '../components/Services.astro';
import Process from '../components/Process.astro';
import CTA from '../components/CTA.astro';
import SectionBlock from '../components/design-system/SectionBlock.astro';
import { frontmatter as siteLabels } from '../content/site-labels.md';

const { siteMeta } = siteLabels;
---

<Layout title={`Programs — ${siteMeta.title}`}>
  <Header activeIndex={2} />
  <main>
    <div class="page-hero">
      <div class="container">
        <p class="page-hero__eyebrow">Programs</p>
        <h1 class="page-hero__title">Find the right program for where you are right now.</h1>
        <p class="page-hero__sub">Whether you're looking for a single session to get clear, or a deep-dive transformation, there's a program designed for you.</p>
      </div>
    </div>

    <Services />
    <Process />

    <!-- FAQ section -->
    <SectionBlock tone="light" padding="default" topBorder={true} className="faq">
      <div class="container">
        <p class="section-label">Common Questions</p>
        <h2 class="section-title" style="margin-bottom: 48px;">EVERYTHING YOU NEED TO KNOW</h2>
        <div class="faq__list">
          {[
            { q: "How do I know if coaching is right for me?", a: "If you have a goal you're not reaching, a challenge you can't seem to get past, or a decision you can't make — coaching is likely the right next step. The free call is a great way to find out." },
            { q: "What's the difference between coaching and therapy?", a: "Coaching is focused on the future — goals, action, and growth. Therapy focuses on the past and mental health. Many of my clients have therapists too; coaching and therapy work well together." },
            { q: "How quickly will I see results?", a: "Most clients feel a shift within the first few sessions. Significant results vary — some people experience major breakthroughs in weeks, others see change unfold over several months. It depends on the goal and the work you put in." },
            { q: "Do you offer refunds?", a: "Single sessions are non-refundable after they're held. For packages, I offer a full refund if you're not satisfied after the first session. I want you to feel confident in your investment." },
          ].map((item) => (
            <details class="faq-item">
              <summary class="faq-item__q">{item.q}</summary>
              <p class="faq-item__a">{item.a}</p>
            </details>
          ))}
        </div>
      </div>
    </SectionBlock>

    <CTA />
  </main>
  <Footer />
</Layout>

<style>
  .page-hero {
    background: var(--surface-base);
    padding: 120px 0 80px;
    border-bottom: 1px solid var(--border-color);
  }

  .page-hero__eyebrow {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--brand-primary);
    margin-bottom: 16px;
  }

  .page-hero__title {
    font-family: var(--font-title);
    font-size: clamp(2rem, 3.5vw, 3rem);
    font-weight: var(--fw-headline);
    color: var(--text-primary);
    line-height: 1.2;
    max-width: 680px;
    margin-bottom: 20px;
  }

  .page-hero__sub {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    max-width: 560px;
    line-height: 1.7;
  }

  :global(.faq) {
    background: var(--surface-base);
  }

  .faq__list {
    display: flex;
    flex-direction: column;
    gap: 0;
    max-width: 800px;
  }

  .faq-item {
    border-bottom: 1px solid var(--border-color);
    cursor: pointer;
  }

  .faq-item:first-child {
    border-top: 1px solid var(--border-color);
  }

  .faq-item__q {
    font-family: var(--font-title);
    font-size: var(--fs-h4);
    font-weight: var(--fw-bold);
    color: var(--text-primary);
    padding: 24px 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    list-style: none;
    cursor: pointer;
    transition: color var(--transition);
  }

  .faq-item__q::-webkit-details-marker {
    display: none;
  }

  .faq-item__q::after {
    content: '+';
    font-size: 1.5rem;
    font-weight: 300;
    color: var(--brand-primary);
    flex-shrink: 0;
  }

  .faq-item[open] .faq-item__q::after {
    content: '−';
  }

  .faq-item[open] .faq-item__q {
    color: var(--brand-primary);
  }

  .faq-item__a {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    line-height: 1.7;
    padding: 0 0 24px;
    max-width: 640px;
  }
</style>


======================================================================
// REFERENCE: coach-pro/pages/contact.astro
======================================================================
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SectionBlock from '../components/design-system/SectionBlock.astro';
import BookingWidget from '../components/widgets/BookingWidget.astro';
import LeadCaptureWidget from '../components/widgets/LeadCaptureWidget.astro';
import { frontmatter as siteLabels } from '../content/site-labels.md';

const { siteMeta } = siteLabels;
---

<Layout title={`Contact — ${siteMeta.title}`}>
  <Header activeIndex={3} />
  <main>
    <div class="page-hero">
      <div class="container">
        <p class="page-hero__eyebrow">Get in touch</p>
        <h1 class="page-hero__title">Let's talk about what's possible for you.</h1>
        <p class="page-hero__sub">Book a free 30-minute discovery call. No pressure, no pitch — just an honest conversation.</p>
      </div>
    </div>

    <SectionBlock tone="light" padding="default" className="contact-section">
      <div class="container">
        <div class="contact__grid">
          <div class="contact__info">
            <p class="section-label">What to expect</p>
            <h2 class="contact__title">On the free call, we'll:</h2>
            <ul class="contact__list">
              {[
                "Explore where you are right now and what's holding you back",
                "Get clear on what you actually want — the outcome, not just the goal",
                "Discuss which program (if any) is the right fit for you",
                "Answer any questions you have about the coaching process",
              ].map((item) => (
                <li class="contact__list-item">
                  <span class="contact__list-icon">✦</span>
                  <p>{item}</p>
                </li>
              ))}
            </ul>
            <div class="contact__guarantee">
              <p class="contact__guarantee-text">No obligation. If we're not the right fit, I'll tell you — and I'll point you toward someone who is.</p>
            </div>
          </div>

          <div class="contact__form-wrap">
            <LeadCaptureWidget title="Let's Talk" description="Tell me a little about where you are and what you're looking for. I'll come back to you within 24 hours." />
          </div>
        </div>
      </div>
    </SectionBlock>
  </main>
  <Footer />
</Layout>

<style>
  .page-hero {
    background: var(--surface-warm);
    padding: 120px 0 64px;
    border-bottom: 1px solid var(--border-color);
  }

  .page-hero__eyebrow {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--brand-primary);
    margin-bottom: 16px;
  }

  .page-hero__title {
    font-family: var(--font-title);
    font-size: clamp(1.8rem, 3vw, 2.6rem);
    font-weight: var(--fw-headline);
    color: var(--text-primary);
    line-height: 1.2;
    max-width: 540px;
    margin-bottom: 16px;
  }

  .page-hero__sub {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    max-width: 460px;
    line-height: 1.65;
  }

  :global(.contact-section) {
    background: var(--surface-base);
  }

  .contact__grid {
    display: grid;
    grid-template-columns: 1fr 1.3fr;
    gap: 80px;
    align-items: start;
  }

  .contact__title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    font-weight: var(--fw-bold);
    color: var(--text-primary);
    margin-bottom: 28px;
  }

  .contact__list {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-bottom: 32px;
  }

  .contact__list-item {
    display: flex;
    gap: 14px;
    align-items: flex-start;
  }

  .contact__list-icon {
    color: var(--brand-primary);
    font-size: 0.875rem;
    flex-shrink: 0;
    margin-top: 3px;
  }

  .contact__list-item p {
    font-size: var(--fs-body);
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .contact__guarantee {
    background: var(--brand-primary-light);
    border: 1px solid var(--border-coral);
    border-radius: var(--radius-md);
    padding: 20px;
  }

  .contact__guarantee-text {
    font-size: var(--fs-small);
    color: var(--text-secondary);
    font-style: italic;
    line-height: 1.6;
  }

  .contact__form-title {
    font-family: var(--font-title);
    font-size: var(--fs-h3);
    font-weight: var(--fw-bold);
    color: var(--text-primary);
    margin-bottom: 28px;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  .form-field {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .form-label {
    font-family: var(--font-body);
    font-size: var(--fs-xs);
    font-weight: var(--fw-semibold);
    color: var(--text-secondary);
  }

  .form-input,
  .form-textarea {
    background: var(--surface-card);
    border: 1.5px solid var(--border-color);
    color: var(--text-primary);
    font-family: var(--font-body);
    font-size: var(--fs-body);
    padding: 12px 16px;
    border-radius: var(--radius-sm);
    outline: none;
    transition: border-color var(--transition);
    width: 100%;
    resize: vertical;
  }

  .form-input:focus,
  .form-textarea:focus {
    border-color: var(--brand-primary);
  }

  .form-input::placeholder,
  .form-textarea::placeholder {
    color: var(--text-muted);
  }

  .form-submit {
    font-family: var(--font-button);
    font-size: var(--fs-small);
    font-weight: var(--fw-semibold);
    background: var(--brand-primary);
    color: white;
    border: none;
    padding: 16px 32px;
    border-radius: var(--radius-full);
    cursor: pointer;
    transition: background var(--transition), transform var(--transition);
    align-self: flex-start;
  }

  .form-submit:hover {
    background: #d45530;
    transform: translateY(-1px);
  }

  @media (max-width: 900px) {
    .contact__grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }

    .form-row {
      grid-template-columns: 1fr;
    }
  }
</style>


======================================================================
// REFERENCE: coach-pro/scripts/useVisibilityClass.js
======================================================================
export function useVisibilityClass({ selector, className = 'is-visible', observingClassName = 'is-observing', threshold = 0.2, rootMargin = '0px', once = true, respectReducedMotion = true, revealIfAlreadyVisible = true }) {
  const elements = Array.from(document.querySelectorAll(selector));
  if (!elements.length) return;
  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  if (respectReducedMotion && reduceMotion) {
    elements.forEach(el => {
      el.classList.add(observingClassName);
      el.classList.add(className);
    });
    return;
  }
  const isInViewport = el => {
    const r = el.getBoundingClientRect();
    return r.bottom > 0 && r.top < window.innerHeight;
  };
  elements.forEach(el => {
    el.classList.add(observingClassName);
    if (revealIfAlreadyVisible && isInViewport(el)) {
      window.requestAnimationFrame(() => window.requestAnimationFrame(() => el.classList.add(className)));
    }
  });
  const observer = new IntersectionObserver(
    entries => entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add(className);
        if (once) observer.unobserve(entry.target);
      } else if (!once) {
        entry.target.classList.remove(className);
      }
    }),
    { threshold, rootMargin }
  );
  elements.forEach(el => observer.observe(el));
}


======================================================================
// REFERENCE: coach-pro/content/site-labels.md
======================================================================
---
siteMeta:
  title: "Your Name — Coach & Transformation Partner"
  description: "Coaching for high achievers ready to break through, lead with purpose, and build the life they actually want."
header:
  logo: "Your Name"
  navLinks:
    - label: "Home"
      href: "/"
    - label: "About"
      href: "/about"
    - label: "Programs"
      href: "/services"
    - label: "Contact"
      href: "/contact"
hero:
  eyebrow: "Certified Coach & Transformation Partner"
  title_html: "THE LIFE YOU WANT IS <em>CLOSER</em> THAN YOU THINK"
  subtitle: "I work with ambitious, high-achieving people ready to stop playing small, make the big moves, and build the future they actually want."
  social_proof: "100+ clients · 94% reach their goals · Fully remote"
  cta_primary: "Book a Free Call"
  cta_secondary: "See Programs"
programs:
  sectionLabel: "Programs"
  sectionTitle: "HOW WE WORK TOGETHER"
  items:
    - title: "Clarity Session"
      description: "A single 90-minute call to identify exactly what's blocking you and create a clear action plan. No fluff."
    - title: "Monthly Coaching"
      description: "Weekly 1:1 sessions, between-session support, and a custom roadmap. For people ready to build lasting momentum."
    - title: "90-Day Transformation"
      description: "Deep-dive intensive. Weekly sessions, unlimited messaging, and a full framework built around your goals."
    - title: "Group Coaching"
      description: "Join a cohort of driven people. Shared energy, accountability, and expert guidance at lower cost."
    - title: "Career Transition"
      description: "Navigating a pivot, promotion, or new direction? A 6-week sprint to get to your next level with confidence."
    - title: "Executive Presence"
      description: "For leaders who want to show up with more authority and impact. Presence is a skill — and it can be trained."
process:
  sectionLabel: "My Approach"
  sectionTitle: "HOW I HELP YOU CHANGE"
  items:
    - number: "01"
      title: "Start with where you are"
      description: "Radical honesty about your current situation, what's working, what's not, and what's actually in the way."
    - number: "02"
      title: "Get clear on where you're going"
      description: "Most people haven't actually defined what they want. We fix that first. Clarity makes everything faster."
    - number: "03"
      title: "Build the plan"
      description: "A realistic, energising 90-day roadmap with weekly milestones and built-in flexibility."
    - number: "04"
      title: "Execute with support"
      description: "Weekly sessions, between-session voice notes, and real-time support when things get hard."
    - number: "05"
      title: "Track and celebrate"
      description: "Progress is measured, acknowledged, and built on. Momentum compounds. So does confidence."
testimonials:
  - quote: "I finally left the job I hated. Now I run my own business. The coaching was the turning point."
    authorName: "Sarah M."
    authorRole: "Former Marketing Director, Now Founder"
  - quote: "I came in stuck and overwhelmed. Six weeks later I had a promotion, a plan, and my confidence back."
    authorName: "James T."
    authorRole: "Senior Product Manager"
  - quote: "The clarity session alone was worth it. More direction in 90 minutes than two years of trying alone."
    authorName: "Rachel O."
    authorRole: "Entrepreneur"
  - quote: "Thought I knew what I wanted. Turns out I was settling. This coaching helped me aim higher — and get there."
    authorName: "David W."
    authorRole: "Director, Financial Services"
stats:
  items:
    - number: "100+"
      label: "Clients Coached"
    - number: "94%"
      label: "Goal Achievement Rate"
    - number: "6"
      label: "Years Coaching"
    - number: "4.9★"
      label: "Client Satisfaction"
cta:
  title: "Your breakthrough starts with one conversation."
  description: "Let's talk about what you actually want and what's in the way. No pitch — just honesty."
  primary: "Book a Free Call"
  secondary: "See Programs"
footer:
  logo: "Your Name"
  tagline: "Coaching for ambitious people ready to build the life they actually want."
  copyrightSuffix: "Your Name Coaching. All rights reserved."
aboutPage:
  heroTitle: "I HELP PEOPLE WHO ARE READY TO FINALLY GO FOR IT"
  intro:
    - "I've spent years helping ambitious people break through the things holding them back — whether that's fear, uncertainty, the wrong environment, or just not knowing what they actually want."
    - "My coaching sits at the intersection of honest conversation and practical strategy. I help you see clearly, decide deliberately, and move with genuine confidence."
  values:
    - "Radical honesty over comfortable reassurance"
    - "Practical plans over endless exploration"
    - "Momentum over perfection"
    - "Your growth over my process"
  faq:
    - question: "Is coaching therapy?"
      answer: "No. Coaching is forward-focused — we work on goals, decisions, and action. If you need therapeutic support, I'll tell you. They can work beautifully alongside each other."
    - question: "How long does coaching take?"
      answer: "It depends on your goals. Clarity sessions are one-off. Most transformation work happens over 3–6 months. Some clients continue for a year or more."
    - question: "Do you offer a free consultation?"
      answer: "Yes. A 20-minute call to see if we're the right fit. No pressure, no pitch. If I can help, I'll tell you. If I can't, I'll tell you that too."
    - question: "How do we meet?"
      answer: "All sessions are via video call. I work with clients across multiple time zones and continents."
servicesPage:
  heroTitle: "PROGRAMS DESIGNED FOR PEOPLE WHO ARE SERIOUS ABOUT CHANGE"
  description: "Each program is built around you — your goals, your timeline, your way of working."
  faq:
    - question: "Which program is right for me?"
      answer: "Start with a Clarity Session if you're unsure. It's designed to help you figure out exactly what you need — and whether working together makes sense."
    - question: "Can I switch programs mid-engagement?"
      answer: "Yes. If your situation changes or you need more support, we can adjust. Flexibility is built in."
contactPage:
  heroTitle: "LET'S START WITH A CONVERSATION"
  intro: "No pitch, no pressure. Just an honest conversation about where you are and whether I can help."
  email: "hello@example.com"
  phone: "+1 (555) 000-0000"
  location: "Remote — worldwide"
---
