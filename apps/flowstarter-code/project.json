{
  "name": "flowstarter-code",
  "projectType": "application",
  "tags": ["app:code"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run build",
        "cwd": "apps/flowstarter-code"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run dev",
        "cwd": "apps/flowstarter-code"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir ../../packages/daytona-utils exec tsc -p tsconfig.json && tsc --noEmit",
        "cwd": "apps/flowstarter-code"
      }
    }
  }
}
