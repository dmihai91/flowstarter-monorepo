{
  "name": "flowstarter-editor",
  "version": "0.2.0",
  "private": true,
  "description": "Flowstarter team + client editor. Hosts the shared Convex deployment (project/thread/checkpoint registry, assets, costs, magic-link access) used by both editor wrappers. The T3-backed Next.js wrapper will live alongside convex/ here — the Remix app that used to occupy this directory has been retired.",
  "type": "module",
  "scripts": {
    "convex:dev": "pnpm exec convex dev",
    "convex:deploy": "pnpm exec convex deploy",
    "convex:codegen": "pnpm exec convex codegen"
  },
  "dependencies": {
    "convex": "^1.31.6"
  },
  "devDependencies": {
    "typescript": "~5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
