---
interface Props {
  question: string;
  answer: string;
}

const { question, answer } = Astro.props;
---

<details class="ds-faq-accordion-item">
  <summary class="ds-faq-accordion-item__summary">
    <span class="ds-faq-accordion-item__question">{question}</span>
    <span class="ds-faq-accordion-item__icon" aria-hidden="true">
      <span
        class="ds-faq-accordion-item__icon-line ds-faq-accordion-item__icon-line--horizontal"
      ></span>
      <span
        class="ds-faq-accordion-item__icon-line ds-faq-accordion-item__icon-line--vertical"
      ></span>
    </span>
  </summary>
  <div class="ds-faq-accordion-item__panel">
    <div class="ds-faq-accordion-item__content">
      <p>{answer}</p>
    </div>
  </div>
</details>

<style>
  .ds-faq-accordion-item {
    display: block;
    width: 100%;
    background: var(--surface-subtle);
    border-radius: var(--radius-lg);
    overflow: clip;
    transition: background-color var(--transition);
  }

  .ds-faq-accordion-item:hover {
    background: var(--surface-subtle-hover);
  }

  .ds-faq-accordion-item__summary {
    list-style: none;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
    gap: 24px;
    padding: 26px 30px;
    cursor: pointer;
  }

  .ds-faq-accordion-item__summary::-webkit-details-marker {
    display: none;
  }

  .ds-faq-accordion-item__summary:focus-visible {
    outline: none;
    box-shadow: inset 0 0 0 2px var(--focus-ring-color);
  }

  .ds-faq-accordion-item__question {
    font-family: var(--font-body);
    font-size: 1.15rem;
    font-weight: var(--fw-semibold);
    line-height: 1.3;
    color: var(--bg-dark);
  }

  .ds-faq-accordion-item__icon {
    position: relative;
    width: 22px;
    height: 22px;
    flex-shrink: 0;
  }

  .ds-faq-accordion-item__icon-line {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 22px;
    height: 2px;
    background: var(--bg-dark);
    transform: translate(-50%, -50%);
    transition:
      transform var(--transition),
      opacity var(--transition);
  }

  .ds-faq-accordion-item__icon-line--vertical {
    transform: translate(-50%, -50%) rotate(90deg);
  }

  .ds-faq-accordion-item[open]
    .ds-faq-accordion-item__icon-line--vertical {
    opacity: 0;
    transform: translate(-50%, -50%) rotate(90deg) scaleX(0);
  }

  .ds-faq-accordion-item__panel {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows var(--transition);
  }

  .ds-faq-accordion-item[open] .ds-faq-accordion-item__panel {
    grid-template-rows: 1fr;
  }

  .ds-faq-accordion-item__content {
    overflow: hidden;
  }

  .ds-faq-accordion-item__content p {
    max-width: 64ch;
    padding: 0 30px 30px;
    color: var(--text-primary);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .ds-faq-accordion-item__summary {
      padding: 22px 20px;
      gap: 20px;
    }
    .ds-faq-accordion-item__content p {
      padding: 0 20px 22px;
    }
  }
</style>
