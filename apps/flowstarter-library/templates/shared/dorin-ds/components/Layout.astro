---
import '../tokens.css';
import '../reset.css';
import DSHead from './DSHead.astro';

type TemplateSlug =
  | 'coach-pro'
  | 'therapist-care'
  | 'freelancer-portfolio'
  | 'fitness-coach'
  | 'creative-portfolio';

interface Props {
  template: TemplateSlug;
  title: string;
  description?: string;
}

const { template, title, description } = Astro.props;

const fontPairFor: Record<TemplateSlug,
  | 'sora-plus-jakarta'
  | 'fraunces-source'
  | 'oswald-manrope'
  | 'space-grotesk-jet'
  | 'playfair-manrope'
> = {
  'coach-pro': 'sora-plus-jakarta',
  'therapist-care': 'fraunces-source',
  'fitness-coach': 'oswald-manrope',
  'freelancer-portfolio': 'space-grotesk-jet',
  'creative-portfolio': 'playfair-manrope',
};

const fontPair = fontPairFor[template];
---

<!doctype html>
<html lang="en" data-template={template}>
  <head>
    <DSHead title={title} description={description} fontPair={fontPair} />
    <slot name="head" />
  </head>
  <body>
    <slot />
  </body>
</html>
