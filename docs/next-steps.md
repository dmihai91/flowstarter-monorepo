Next steps:
- Convex schema refactor to handle conversation history, current project state, collect and sync business details with the supabase db and pass them to the agent
- Agent orchestration refactoring to leverage lang graph capabilities together with our custom gretly engine and flow ops and also leverage what agents sdk provides so we can have a more robust and flexible agent orchestration system. The goal is to have a stable agent that can build a site from a template and let our team and our clients to modify it. The client should have access to a limited version of the editor while our team has access to the full editor. Also the system should support indexing of the templates + the site code so the agent can build faster and more accurately. Also we should think of a diffing system + lockers for the changes our team or clients makes to the site so we don't overwrite each other's changes.
- Fix main platform to editor sync so a project started on the main platform is available in the editor. A project is always started in the main platform and then synced to the editor.
- One more thing we need to think to streamline the platform + editor code to be leaner and only contains what's needed for the launch. If we can achieve all we want under 50k lines of code, we should do it.