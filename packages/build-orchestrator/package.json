{
  "name": "@flowstarter/build-orchestrator",
  "version": "0.1.0",
  "description": "The real build engine: a planner brain decomposes the user's prompt into a task graph, waves of coding agents execute it via the Cursor SDK (cursor-agent), the brain validates and replans. Ideas ported from ask-sage (triage/effort gates, ReAct loop) and gretly (planner → dispatcher → worker waves → validator).",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@flowstarter/build-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "@types/node": "20.19.9"
  }
}
